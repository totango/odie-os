# Full-release readiness — remaining work, not rollout authorization

**Latest legacy-drain slice:** [Legacy owner fencing](legacy-owner-fencing.md) supersedes section 2's unresolved Finance/legacy design and earlier token-retirement claims. Finance remains independently configured. Context/JARVIS privileged mutations now check current purpose/generation at actual resource owners; issued Context Git tokens remain independently revocable credentials, not continuing issuer-admin authority. Two complete matching private provider scans are required for managed activation; their domain/version baseline is stored atomically and checked by fresh subsequent readiness. No unconditional legacy blocker was removed, no destructive token migration was added, and no live activation occurred. The three server type errors are fixed using actual source return types. New focused/build evidence is in the slice handoff; full-release/image/pricing/notifier/CI/rollback execution gates below remain separate and must be refreshed.

**Latest correction:** [Finance operator separation](finance-operator-separation.md) removes Finance guard/descendant-drain gates, not actual provider legacy-child/token gates. Finance is deployment-configured independently of managed admins, with all four current identities preserved by omitted-only ADMINS fallback. Managed activation and AutoBuild remain blocked for concrete Context/JARVIS legacy inventory and other actual gates. Focused runtime tests pass, but fresh backend build/authority-test typing fails at three native-result inference sites. All prior full build/test/dryrun artifact counts below are stale for notifier/Finance changes. Review and fresh release gates are required; no commit/deploy/rollback execution in this slice.

## Accepted checkpoint and release boundary

Owner direction: **complete every task, then commit/merge/deploy all changes together; no partial board rollout**. The reviews' conditional static-board rollout assessment is not the selected release plan. No stage/commit/push/PR/deploy, live grants, credentials reads, provider writes, Slack messages or paid model calls are authorized now. Preserve all four configured admins (Jacob, Keith, Nick, Stacy), exact account-scoped identity, private Context, hidden JARVIS management UI, ordinary Code Sessions, `totango/odie-os` / `main`, U1 and the reviewed dependency patch/graph.

Final independent source/evidence review **PASS**; final security review **BLOCKED for managed activation/full acceptance**, not a new source defect. Both reviews were read from their structured JSON outputs: the configured `292cebf0-58ea-4e42-8661-63950a99526c/final-gates/{code-evidence,security}.md` files are empty (artifact-routing deficiency). Exact review provenance is below. Latest local evidence remains **194 root +3349 package tests /7 existing skips**, isolated **87 tasks /zero cache hits**, **16 dry-run outputs**. These are prior verified results, not commands rerun for this plan. Native/type diagnostics remain failed and excluded from those claims.

## Ordered, testable remaining work

Work packages 1 and 3 can advance while 2 awaits a security design decision; 4 can assemble its isolated harness meanwhile but cannot claim legitimate managed success before the genuine proof paths exist. All converge at 5 and 6. “Local” means ownable repository implementation, not permission to bypass runtime protections.

### 1. Slack evidence; separately, optional generic deployment wiring

**Local source:** `gatekeeper-jarvis/src/request-build-notifier.ts:configuration/requestBuildNotifierReadiness/sendRequestBuildNotification`, shared `request-build-notification.ts`, backend `community-requests.ts:activationReasons`, `request-build-outbox.ts`. Readiness currently checks only protocol/config presence; destination proof is not implemented. Add bounded, strict Slack-owned identity/channel verification: confirm the documented `auth.test` and `conversations.info` response/scopes contract first, with `conversations.members` only if the accepted membership evidence requires it. These are proposed read seams, not already verified provider responses. Bind evidence to the current dedicated credential, bot/team, fixed channel and Workshop origin, with freshness and rotation/config-change invalidation; no env confirmation flag or browser-supplied attestation. Keep token and private destination details out of public readiness/logs. Check at admission/send as appropriate so stale evidence cannot authorize a changed destination. Reuse current fixed-link send/ambiguity semantics.

**Acceptance:** local fake HTTP at the real JARVIS service boundary proves matching bot/team/channel and membership; wrong team/channel, non-bot credential, inaccessible/archived channel, missing scope, rotation, stale evidence, malformed/oversized/redirect/timeout responses all deny without postMessage. Current outbox acknowledgement loss, 429-only retries, duplicate/privacy and ambiguous-no-resend regressions stay green. Production `NOTIFIER_DESTINATION_UNVERIFIED` is replaced only by this genuine mechanism, never by test success/config presence.

**Owner/provider evidence:** verify intended workspace, bot identity, channel membership and required read/post scopes through approved Slack-owned reads using a dedicated secret provisioned by the operator. Historical `C09EW0T5VB5` is only a candidate, not confirmation. A real send is a later separately approved action. No price/channel/token/scope response is invented here.

**Generic release is a different seam:** `scripts/request-build-notifier-binding.ts` already conditionally binds dev JARVIS; Odie backend/JARVIS production configs have the private entrypoint/browser-origin wiring. `scripts/release/manifest-lib.ts:WorkerEntry.gatekeeperBindingExpansion/buildWorkerEntry` supports only ordinary installed gatekeeper expansion, not an optional second private notifier entrypoint. Ownable work is an explicit optional-binding manifest contract, matching golden/installation-removal tests and correct SPA-origin/dedicated-secret inputs. Agree the contract with the external deploy-service renderer owner before changing it: backend gets `REQUEST_BUILD_NOTIFIER` only if JARVIS is installed; router/ambient catalog get no private entrypoint; removal/reconciliation removes it; existing releases stay compatible. Renderer implementation/consumer tests are outside this checkout. Do not add an unconditional service binding, force JARVIS installation, guess a manifest version/placeholder, or claim a manual dashboard binding survives reconciliation. No package-manifest/root-graph change is authorized in this stage.

### 2. Finance containment and real legacy drain — unresolved architecture, not credentials

Supervisor confirms **no approved complete Finance design exists**. Reuse the existing ADR/authority-blocker/prototype inventory; no further native/type investigation or speculative guard wiring. The finite next in-repo deliverable is only the current-seam delta and a supervisor-mediated design decision, followed by implementation if a complete supported design is approved.

Current provenance/escape inventory:

- `overseer.ts:#financeRole` (7382), `hasFinanceHubAccess/open`: validated Finance claim/owner record, then boolean admin bypass yields `build`; exact owner independently retains `build`, direct-owner share independently yields `use`. `server.ts:#openGadgetInternal` still passes static admin status. Guard only authority derived from elevation; revocation must permit a fresh ordinary owner/direct-share reopen, not retain elevated build through a cached role.
- `OverseerClientInterface` (8382) hands out editor/chat/gadget/gatekeeper capabilities. `GadgetClientImpl.connectToGadget/export/getBinding` (10507–10547) escapes arbitrary gadget facets, streams and binding children; `GatekeeperClientImpl.openSession` (10846) returns arbitrary provider sessions, not a closed Workshop type graph. `ApprovalQueueImpl.bindHook` forwards native callbacks; retained grandchildren, callback returns/arguments, maps/pipelines and both stream directions must stay in the same admission boundary after parent disposal.
- `user.ts:startAccountAppUi` retains legacy boolean dispatch; `startAccountAppUiAuthorized` uses persisted `connectedAccounts` description/account references and never retries on denial. Context `library-gatekeeper.ts`/`context-api.ts` and JARVIS `index.ts`/`policy.ts` retain old boolean UI compatibility. Stored native account references can pin old providers; old UI URLs/stubs, older AdminApi capabilities and Finance sessions/descendants are unfenced legacy inventory. Protocol advertisement/new deployments are not proof they disappeared.
- `overseer.ts:scheduleRevocationRestart` (3763) flushes, waits100ms, aborts its DO. `server.ts` notifyClosed disposal/WS session abort does not acknowledge all descendants; HTTP-batch lifetime is different. None establishes transitive provider/UI/native drain. No persisted acknowledged complete drain mechanism currently exists.
- `admin-authority.ts:AdminAuthority` production evidence callback (192–193) still throws; `AdminAuthorityState.activate` has the real epoch/revision/bootstrap transaction. `community-requests.ts:activationReasons` (132) independently hard-denies Finance/drain. Replace neither until implemented containment and real covered-surface drain controls supply backend-owned evidence with invalidation semantics.

**Finite design acceptance matrix (required before production wiring):** (a) a supported public-API boundary covers every retained descendant/callback/stream and both directions; checks ordered after revoke commit deny, an already admitted protocol message including map may finish; (b) parent disposal, forwarding, independent sessions, regrant, restart and authority outage cannot revive old generations; (c) pending checks/queues are bounded, real cancellation/disposal and capacity ownership are accounted for without manufactured counters, wake bytes or swallowed rejection; (d) actual Finance owner/direct-share behavior, WebSocket/HTTP batch, arbitrary provider native pass-back and inferred callbacks remain compatible and type-safe; (e) old accounts/provider versions/legacy UIs/AdminApi/Finance exports are fenced/retired and acknowledged drained, with epoch/revision-scoped evidence that invalidates on change. A namespace restart or refresh request satisfies none of (e) alone.

**Precise preserved native issues:** full prototype32 assertions plus3 unhandled native-stream rejections exits1; direct native `pipeTo` reproduces an extra rejection whose private owner remains unknown. Pool and standalone cancellation/explicit-pump controls did not observe producer cancellation despite actual target disposal. Source research identified producer-read-dependent disconnect observation; one diagnostic byte woke cancellation, but an idle producer can wait indefinitely; forced SIGKILL after the five-second shutdown bound is not graceful drain. Restored full prototype types have7 TS2345 +2 TS2339; rejected declaration experiments fail unannotated/mixed callback inference and retain an invalid private-target callback acceptance. The13-test private-lifetime pass and repaired `.dup()` do not solve these. A supported runtime/library fix may be necessary and is decision-dependent (including hosted-runtime availability); no upgrade/native replacement, third patch, suppression, restricted Finance feature set or new experiment is authorized.

### 3. Pinned image, enforced egress/cleanup, conservative numeric pricing

**Local source/test work:** extend existing `gatekeeper-sessions/pi-image/smoke-image.sh` and the generated `request-build-runner.ts` program with a restricted-run smoke using an owned empty resource loader and local fake model. `Dockerfile.pi`/image lock now target Pi0.85.1/OpenCode1.18.30/official Prime0.9.4; keep Sandbox SDK/base pins. The current source adds Prime REPL protocol-3 and MCP2 `input_schema` smoke coverage, but the actual built image still must be tested (38 host/image transitive differences remain). Record exact image/source/lock provenance, amd64 ABI/versions, actual runner exit/patch bounds and absence of personal plugins/skills/context/editor control. Existing source-level image smoke does not exercise the Worker/DO Sandbox path, and this slice skipped local image build because disk preflight showed only 9.0Gi free.

**Infrastructure approval needed before running:** an isolated actual image build needs approved container tooling/acquisition/network/cache policy; no such execution is authorized by this docs step. Installed SDK has no supported direct-local client for `/container-server/sandbox` (image README). Propose supported local Worker/Container integration if available; otherwise its native SDK proof requires a separately authorized candidate Worker/DO canary. Do not invent a local private-API substitute. Neither dry-run Worker artifacts nor host bootstrap attests image activation.

**Acceptance:** actual `RequestBuildSandbox` and `CodingSessionPolicy.forwardRequestBuildEgress/forwardTeamPiCodexRequest` prove only exact-repo upload-pack reads and approved dependency/model routes; deny write/other-repo/redirect/header-credential injection/MCP/catalog/arbitrary internet and ordinary attach/editor paths. Adversarial generated subprocesses cannot acquire publication/Workshop/provider secrets. Exercise actual deadline/cancel, lost process-start ack, stale generation, destroy failure/retry and descendant process teardown via `request-build-execution.ts` and Sessions `destroy`; capacity releases only after exact-generation cleanup, not a wait timeout. Provider/container behavior not reproducible locally remains an explicit canary gate.

**Pricing is additional implementation:** `request-build-policy.ts:parseRequestBuildPolicy/boundedBuildModelPayload`, `sessions.ts:forwardTeamPiCodexRequest` (usage reservation754–765), `requestBuildReadiness` and `user.ts:requestBuildEligibility` already validate finite values, reserve before fetch and check account entitlement. They do not prove `callChargeMicros` is conservative. Add trusted reviewed tariff/model-revision matching and fail-closed freshness/change checks to the backend readiness path. Acceptance requires a documented upper bound for all billable input (including tool/schema/framing overhead), output/reasoning and relay fees at enforced limits; bytes are not an assumed exact token conversion. Bound/deny unsupported metering/features; use integer ceiling arithmetic, safe totals and no discounts/refunds for uncertainty. Tests cover boundary/exhaustion/concurrent admissions, oversize/transformed input, missing/stale tariff/model mismatch, crash/timeouts, no refund, no forwarding after exhaustion. Numeric values require authoritative provider/relay pricing and operator budget approval; no remembered rate or token-price default is supplied. Current actor GitHub write entitlement, installation read/publisher scopes, exact-main SHA and model entitlement require genuine provider evidence, not config presence.

### 4. Authenticated browser → real facade → managed-success integration

**Local task:** extend `workshop-backend/__tests__/request-build-api.test.ts`, `__tests__/request-build/{fixture.js,control.test.ts}` and frontend `community-requests/RequestBuildPanel`/run route into one isolated browser journey through the real authentication/WebSocket facade, AdminApi, board storage, authority generations, private Sessions protocol, publisher and real JARVIS receiver. Existing success tests instantiate real controllers with test-owned dependencies; facade tests prove safe denial; jsdom tests use API doubles. Connecting those is not merely adding another controller assertion.

Keep fixture-only identities/external transports outside production constructors/env/RPC. Build the harness now using installed tools where supported; any new browser runtime/launch/protection need goes to supervisor. A genuine-success claim requires the eventual approved Finance/readiness implementations with fixture-owned controls that actually establish the local facts, not injected `true` proof fields or direct SQL managed-mode mutation. Until then retain the real production facade's hard-denial test and explicitly incomplete success gate.

**Acceptance:** signed-out denial; exact existing admin signs in, reviews frozen revision, starts one run and loses/retries ack with the same key; browser disconnect/reconnect does not own progress; actual receipt→validated draft PR→fixed Slack links and safe second-account run view; cancel/hide/revoke/outage deny future work while cleanup/reconciliation survives; no secrets/control stubs/author IDs in public views. Separately combine actual-image execution evidence from3; fake execution is never relabeled image proof. Later owner-approved production-safe checks first cover auth/navigation/readiness/private-Context/ordinary-Code/JARVIS-hidden behavior without writes; any managed start/revoke/PR/message/model canary requires separately named test principal, destination, budget and cleanup authorization. Do not test by altering the four production admins.

### 5. Clean isolated CI parity and vulnerability readiness

After remaining source/fixtures and independent reviews, rerun affected source/test-program checks and exact root lint/build/test, then fresh isolated artifacts with current source hashes. Use only the approved strict runner described below. Prior1541-source-file acquired-graph copy/87 fresh tasks/16 artifacts is not clean installation or Linux parity.

`ci.yml` uses Ubuntu/Node24.19.0/setup-vp install; local evidence is Darwin/Node24.14.1/noCI. The previous privileged Wrangler mismatch was repaired in source: publish/deploy now check out exact source, enable Corepack, install the frozen workspace with setup-vp before Cloudflare credentials are present, and invoke `pnpm exec wrangler` from the lock-selected `wrangler 4.131.1` instead of a global npm install. Clean fresh-store installation, CI-mode or Linux runtime still requires a separately approved isolated environment. Test patched24 Cap'n Web0.12 links +2 legitimate0.8 consumers and frozen lock on that clean graph, not a manually copied unpatched base. Remote CI later belongs to owner-authorized release work.

Current raw public-registry audits were run with empty user/global npm configs. Image npm production audit still fails with 2 high findings: official Prime0.9.4 -> `extract-zip 2.0.1` and `prime-agent`; npm publishes no `extract-zip 2.0.2`. Root pnpm production audit still fails on Monaco-pinned DOMPurify, `@cloudflare/puppeteer 1.4.0` -> `@puppeteer/browsers 2.2.4` -> `extract-zip 2.0.1`, Vite/PostCSS `nanoid 3.3.16`, and `@cloudflare/sandbox` -> `hono 4.13.1`. Root all-dependency audit additionally reports dev-only direct Miniflare `undici 7.28.0`, pool0.22.0/Miniflare `sharp 0.35.2`, and out-of-contract `gatekeeper-odie-kg` Vitest4.1.10. Separately scoped follow-up is still needed for the out-of-contract direct Miniflare package files (`gatekeeper-cloudflare`, `gatekeeper-scheduler`, `gatekeeper-zendesk`) and any owner-approved residual-risk disposition. Do not run an automatic fix/install/upgrade or assert no vulnerabilities from absent data.

### 6. Migration-aware all-change release handoff

Prepare, do not execute: coherent review groupings for kernel/shared authority/builds, providers/image, UI, U1/two-hunk dependency patch, and release/docs; preserve all accumulated unrelated-to-this-step edits and exclude secrets/scratch/evidence junk from eventual commit selection without deleting historical evidence. Empty index remains required now. Do not merge broader upstream or reinterpret source PASS as release approval.

Acceptance requires final source/security review, complete1–5 evidence, exact tree/commit/artifact/image correspondence, vulnerability disposition, approved four-account bootstrap preview and supported provider/legacy drain procedure. Preserve ordered backend v4-community-requests/v5-admin-authority (base v3 NativeBrowserFlow vs production empty v3), Sessions v6-request-build after v1–v5, and all additive SQL history. Verify generic golden and Odie actual deploy order, optional bindings and same browser origin; provider-before-consumer sequencing is inside **one fully ready release**, not permission to release the board early. Pin image only after actual candidate evidence.

Owner/release operator then authorizes commit/merge into canonical `totango/odie-os main`, remote CI and a single release rollout. Record preflight/live exact identities, capability drain and activation revision before managed transition; source cannot confirm live membership. Persistent DO migrations are not reversed by code rollback. Before activation retain a schema-compatible legacy/prepared fallback; after managed activation never roll back to static authorization or erase grants/generations/drain history—retain enforcement, stop new admissions and roll forward. Retain prior production image and schema-compatible worker artifacts, reconcile already-sent PR/Slack and exact-generation cleanup; rollback cannot unsend writes. The final production check must preserve owner/direct-share Finance and private Context/ordinary Code access and keep JARVIS hidden.

## Evidence/protection references

- Final verification: `/Users/jacob_1/.pi/agent/sessions/--Users-jacob_1-odie-os--/subagent-artifacts/outputs/057346eb-e1d7-4c30-baeb-07c7cb253db3/closeout/final-verification.md`. Artifact manifest SHA256 `d58e54563e6eac2c299f75813578e3c64480348ede9be7eabaf60319c545e0fd` (131 files/57,257,669 bytes). Historical command failures remain there and in component docs.
- Code review JSON: `/var/folders/5q/r1rw9tjx4pl2phchr2rhc_lm0000gp/T/pi-subagents-uid-502/async-subagent-runs/53cabc22-467f-4c9f-aa12-b99e758a229b/structured-output/pi-subagent-structured-DahuRy/output.json`.
- Security review JSON: `/var/folders/5q/r1rw9tjx4pl2phchr2rhc_lm0000gp/T/pi-subagents-uid-502/async-subagent-runs/faddb8e8-6a51-437f-93ce-4bdc1c298854/structured-output/pi-subagent-structured-v8XfBO/output.json`.
- Runtime/test invocation only after approval: `/usr/bin/python3 /tmp/odie-restricted-sessions-evidence/run-builds-pr-candidate.py <unique-label> pnpm --config.verify-deps-before-run=error ...`, runner SHA256 `56d83de010ec5fb47e4d1f9c6da002a9a56804f7607d7de21ad8326da551f551`. Preserve original store/noCI/install monitor/private-read/network guards. Original `run.py` monitor is obsolete. No reused artifact derivative without explicit approval, install, private credential reads or cache exception.
- Dependency evidence remains71337 unchanged records +1 previously approved validation timestamp, state SHA256 `66fff93882427af045cc728c481778fe08f1ffd37b4764c5df8ec67784a8293e`; not re-audited in this documentation-only pass. HEAD `248cc75eda989d5ddc93507518a0bf284065e93f`, branch `feat/community-requests`.

This plan performs no runtime/native investigation and supplies no positive production evidence. Historical component checkpoints remain historical; current release readiness is the six packages above, not a permanent disabled placeholder and not a generic “credentials missing” block.
