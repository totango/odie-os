# End-to-end implementation progress

## Latest frozen owner-fencing slice (supersedes Finance-coupled requirements below)

- [x] Separate exact deployment Finance operators from managed Workshop administrators; optional `FINANCE_OPERATORS`, omitted-only `ADMINS` fallback, explicit empty grants none; preserve all four configured seeds, aliases, ordinary sessions and owner/direct-share access.
- [x] Fence Context public-management and JARVIS policy mutations at their durable resource owners with purpose/current-generation checks.
- [x] Define already-issued Context Git tokens as independently revocable write credentials, not administrator authority and not automatically retired on admin removal.
- [x] Require two fresh matching binding-owned Context/JARVIS scans before managed activation; atomically persist and subsequently recheck the immutable provider/domain/fence-version baseline.
- [x] Resolve the backend/authority-test-program native-result inference diagnostics using source-derived types, with no native/dependency workaround.
- [x] Independent resource-owner security review PASS at LOW risk for this frozen source scope.
- [ ] Refresh complete release tests/build/artifacts, image/candidate canary, authoritative pricing/model/repository and Slack evidence, advisory disposition, authenticated integration, and rollback/live checks. Dependency/release repair current-source checks pass and workflows are hardened, but audits and image smoke remain blocked/open. Nothing is staged or deployed.

Current contracts and rollback limits: [finance-operator-separation.md](finance-operator-separation.md), [legacy-owner-fencing.md](legacy-owner-fencing.md), and [full-release-readiness.md](full-release-readiness.md). Older Finance-containment and unfenced-provider TODOs below are historical and superseded.

Implementation approved with safeguards in `implementation-decisions.md`. No staging/commit/push/merge/deployment/external messages authorized.

## Current full-release gate (supersedes historical pending-stage statements)

**Owner: complete ALL tasks before committing/merging/deploying ALL changes together; no partial board rollout.** Final source/evidence review PASS and final security BLOCKED for managed activation/full acceptance were read from the actual structured review JSONs; configured final-gates markdown files are empty. Exact review provenance and ownable source seams/test acceptance/operator actions: [full-release readiness](full-release-readiness.md). Prior root194 +packages3349/7 skips, isolated87/zero hits and16 artifacts remain the latest local verification, not a new run here.

- [x] Final Stage E ES2022 repair/source-evidence/artifact correspondence independent review PASS.
- [x] Final security source inspection of board/authority/controller/restricted Sessions/publisher/notifier/UI completed; no new blocking source defect reported. This checkbox is **not** managed activation acceptance.
- [x] Private Slack identity/destination/access mechanism and invalidation tests implemented locally; [notifier checkpoint](notifier-destination-verification.md). Production computes only the notifier blocker, not posting permission. Controller/publisher48 (15 added), facade/authority23 and JARVIS39 pass; affected types/scoped lint pass. Independent review pending; full-root totals and16 dryruns above are now pre-change source evidence. Full-suite RPC-result disposal warning attribution remains unresolved and unsuppressed.
- [ ] Independent notifier source/security review, genuine operator-authorized credential/destination check and separate send smoke. Separately agree/implement optional generic notifier binding with external deploy-service consumer support.
- [ ] Finite Finance current-inventory delta and supervisor-mediated complete security design decision, then supported implementation/verification and true legacy drain. Supervisor confirms no complete approved design exists; no speculative wiring or repeated native/type research.
- [ ] Actual pinned-image restricted runner/egress/process cleanup evidence; conservative numeric tariff/model readiness implementation and provider/budget approval. Latest local Docker preflight showed only6.5Gi free, so the requested linux/amd64 image build/network-none smoke was not attempted to avoid unsafe exhaustion.
- [ ] One authenticated browser → real facade → managed-success local isolated journey with genuine local proof controls, then separately authorized production-safe checks; no production fixture override.
- [ ] Final clean isolated CI/toolchain parity, advisory disposition, fresh source/security review and migration-aware all-change commit/merge/deploy/rollback authorization. Separately scoped follow-up must still bump direct `miniflare` to `5.20260911.0-alpha` in gatekeeper-cloudflare, gatekeeper-scheduler and gatekeeper-zendesk, then regenerate/revalidate the lock.

## DAG

**Latest owner priority:** missing implementation first; no further native/type research. See `implementation-first-addendum.md` and `implementation-replan.md` (supersede ordering, not safety).

`retained board/U1/patch -> A authority + known privileged paths/admin UI -> B restricted Sessions -> C durable builds/trusted PR -> D Slack/run UI -> E review/repair/protected checks`

Finance guard and legacy-capability drain are separate **managed activation** architecture/implementation dependencies, not blanket implementation blockers. AdminAuthority and known privileged-path checks exist; effective membership remains static in legacy/prepared modes. A/UI, Stage C and repaired Stage D have supplied source reviews; final Stage E review PASS supersedes stale review-pending entries. Final security inspected restricted Sessions/controller/notifier/UI but blocks actual activation. Genuine Slack destination verification, supported generic optional wiring, image/pricing and integrated browser proof remain explicit work, not permanently disabled placeholders. Legacy/prepared management cannot claim effective grant/revoke before legitimate managed transition.

Writers serial in a feature branch; fresh read-only reviews between stages. Update this file at stage boundaries with actual files/verification/blockers, not aspirational completion.

- [x] Preflight: Git repository verified; baseline HEAD `248cc75eda989d5ddc93507518a0bf284065e93f`; only pre-existing untracked artifacts.
- [x] User confirmed U1 only, preserve configured admins, account identity, canonical repo/base and fail-closed Auto-Build prerequisites.
- [x] Architecture decision record before source edits.
- [x] U1: baseline scheduler tests, exact upstream test-harness patch, post-change verification/review.
  - [x] Writer: baseline, exact two-file patch and post-change package tests/build verified (evidence below).
  - [x] Independent read-only review PASS (upstream-review stage); broader CI-parity verification remains outstanding.
- [x] Implementation-first architecture checkpoint: supervisor approved transition/compatibility contract before marking docs ready; addendum/DAG updated. Documents-only checkpoint independently reviewed PASS.
- [x] A-core writer: AdminAuthority/backend/API and known privileged paths implemented; real-workerd tests and shipped-source build/lint pass. Four static seeds preserved; managed transition fails closed on private Finance/drain evidence. **First review requested three repairs; repaired source and combined test-program tsc passed local checks and the supplied second independent review.**
- [x] A-UI writer: real administrator membership/audit panel in AdminPage, exact-account grant/revoke/regrant confirmation, static-mode/readiness and lifecycle/error/concurrency tests. Full frontend 684 passed; fresh uncached frontend build/types and targeted provider tests pass. Supplied independent UI review PASS.
- [x] A-core review: supplied second review PASS for the repaired component/scoped typing; lone stale AdminApi check comment corrected. This is not managed Finance activation.
- [x] A-UI review: supplied independent review PASS for UI, lifecycle, exact-account/error/readiness behavior and approved equivalent shared Fetcher type spelling.
- [x] D writer: repository-owned binding-only JARVIS Slack transport, separate durable backend delivery/attempt ledger, real frozen-spec/start/cancel/readiness controls and signed-in privacy-filtered run route/history. Protected real cross-worker/local fake HTTP tests and UI lifetime/control tests; no live actions. See `slack-run-ui-implementation.md` and the Stage D closeout artifact for actual gates and retained authoring failures.
- [x] D review: supplied second-repair independent review PASS for post-commit scheduling/same-key recovery, alarm regressions and retained UI retries/lifetimes. Earlier browser-origin and start-alarm REVISE evidence remains preserved in `slack-run-ui-implementation.md` and `closeout/slack-ui-fix-1.md`. This is not activation or review of the later Stage E compatibility repair.
- [x] D notifier destination mechanism: live-check production HTTP implementation plus protected local fake-Slack tests; no real destination check performed. See current notifier checkpoint above.
- [ ] D activation acceptance: independent notifier review, actual operator-authorized destination/send evidence and supported generic release optional private notifier binding. No env confirmation toggle can replace evidence.
- [ ] Separate activation gate: complete Finance-derived containment and legacy-provider/capability drain. No speculative guard wiring or effective revocation claim.
- [x] D/E writer: request/bug persistence/API/votes/details/search/moderation/privacy and tests implemented and locally verified; independent board-backend-review PASS (supplied stage result).
- [x] F/G board writer: default-on board, static-admin moderation, public bugs/new-only entry point and interaction tests implemented and locally verified; independent board-frontend-review PASS (supplied stage result). Dynamic admin management is now implemented in A-UI; activation remains gated.
- [x] Board integration writer: independently usable board milestone verified with local fixtures; setup-screen navigation gaps repaired. Final edits still require independent review; not a live rollout or full-goal completion.
- [x] B writer: restricted Sessions protocol/registry reservation/SDK runner/collection/egress/budget ledger and workerd tests implemented; source harness targets Pi0.85.1/OpenCode1.18.30. Production numeric policy absent; the original B-only authorization seam is now replaced by C. Old image/Finance/drain/pricing gates remain.
- [x] B security source inspection: final security review inspected restricted runner/egress/credential paths; activation remains BLOCKED on actual image/egress/cleanup proof.
- [ ] B image/harness acceptance: actual pinned-image execution and separate complete lock/advisory disposition remain required; final security inspection is not image attestation.
- [x] C writer: durable backend build control plane, actual private run admission, authenticated projected reads and Worker-owned Git Data/draft-PR publication; actual workerd controllers with mocked providers pass. No activation claim.
- [x] C source review: supplied Stage C component review PASS; final security inspected control/publisher/private credential paths. Native/image/Finance/provider activation proof is not implied.
- [x] D: private Slack outbox/notifier and public run UI, privacy/ambiguity/readiness tests; source review accepted as above, activation implementation gaps remain.
- [x] Independent final security source review completed; managed activation BLOCKED, not a new source-repair finding.
- [ ] Integrated end-to-end/runtime/activation evidence and fresh security acceptance after remaining implementation.
- [x] Local broad build/lint/exact `pnpm test` and sanitized production dry-run artifacts verified (CI closeout evidence below); independent closeout review PASS per `CURRENT-STATUS.md`, limited to those local checks.
- [ ] Clean-install/Ubuntu/CI-mode parity, remote CI/CD and full-product readiness. Local gates do not complete Finance cutover, restricted Auto-Build/Slack activation.

## Evidence / blockers

### Stage E — repaired current-source local gates, full acceptance still BLOCKED

- [x] Concrete cross-layer repair: two shared protocol sorts now mutate only fresh keys/entries arrays for existing ES2022 consumers, with exactly two supervisor-approved documented `unicorn/no-array-sort` line exceptions. Added frozen-value/order/extra-key regressions in `request-build/publisher.test.ts`; no changed runtime semantics, consumer compiler libs, dependency, image, manifest or native research. Corrected stale AGENTS mint-only authorization note.
- [x] Repaired-source exact protected root lint/build/test pass:142 warnings/0 errors;194 root +3349 package tests/7 existing skips; backend task actually selects33 controller/publisher cases. Direct fresh board/authority/facade23, Sessions23, board/run UI24, controller/publisher33 and scoped test types pass. Root no-cache build87 tasks/1 nested hit, isolated fresh build87/0 hits. Counts include the Sessions package's existing generated test copies, not a unique-source claim.
- [x] Current-source16 production dryrun outputs verified:131 files/57,257,669 bytes. Root `/private/tmp/odie-restricted-sessions-evidence/tmp/literal-test-2y0lntow/odie-final-artifact-o4nmvksx/source/production-deploy`; sibling `artifact-verification.json` SHA256 `d58e54563e6eac2c299f75813578e3c64480348ede9be7eabaf60319c545e0fd`.1541 source files,73772 copied input files and2863 confined links audited before/after; original71337+1 metadata exception unchanged, exact patch24+2/U1/HEAD/index preserved. Documentation-only final deltas are outside that snapshot; runtime source/config matches.
- [x] Independent final Stage E repair/evidence review PASS (actual structured JSON cited in full-release readiness); not a self-issued verdict or deployment approval.
- [ ] One authenticated browser→managed-success E2E. Existing local evidence is separately authenticated production-facade denial/board participation, successful real-workerd controllers with test-owned provider/container dependencies, and jsdom UI. This distinction prevents claiming the missing integrated/image proof.
- [ ] Actual restricted image/container execution, egress/process cleanup, Finance containment/legacy drain, numeric conservative pricing/provider readiness, genuine notifier destination verification/generic optional binding support, managed activation and remote CI/clean-install parity. No live action is authorized or claimed.

Failures retained: first copied `.npmrc` stat denied under the strict profile (stopped/escalated, no build); supervisor-approved owned-TMPDIR retry fully audited; the first fresh isolated build then failed on ES2022 `toSorted` typing. Initial fresh-array repair was lint-rejected before the two explicit narrow exceptions were approved. Each passing run has a separate log. Original strict runner SHA56d83... is unchanged; supervisor-reviewed artifact-only derivatives bind exact snapshot/inventory, preserve original monitor/private-read/network guards, add copied immutable graph protection and only the validated copied public `.npmrc` read literal. No broad cache exception, new state timestamp, install or hidden source replay. Exact commands and hashes: `/tmp/odie-restricted-sessions-evidence/final-verification-*`, `commands.jsonl`, and the configured `closeout/final-verification.md` handoff. Earlier failures below are chronological evidence, not current claims.

### Historical durable builds / trusted draft PR checkpoint — review subsequently PASS

See [component evidence](durable-builds-implementation.md). Existing board SQLite owns immutable approval, current-authority/capacity/mutation receipts, alarms, exact Sessions reservation/generation, cancellation and ambiguity-safe publication. Public AdminApi and authenticated safe reads are real. Strict Worker publisher verifies base/blob/diff/full tree/commit/ref/draft PR; sandbox has no publication credential. Permanent pre-reservation cancellation fence closes the delayed-ensure race. Missing production Finance/drain/image/pricing/notifier proofs remain hard denied, without override.

Protected backend Vite task passed **624 +6 +15 tests /4 pre-existing integration skips**, Sessions script **668** (existing generated copies included), affected shipped-source/test-program tsc and root lint passed. New suite is registered in the Vite task/root test path; unchanged direct backend test:run needs the documented explicit extra config command. No new root-wide test/build or artifact claim. Preserve the documented monitor/cache incident and failed probes: actual pnpm performed the separately approved timestamp-only refresh; original strict runner protections/no-write-exception no-op then passed. Current full integrity is **71337 exact dependency/shim records +1 approved metadata-only timestamp**, exact patch24+2/U1/manifests/HEAD/empty index. No install, staging, deployment or live provider action.

### Historical restricted Sessions checkpoint — final source security inspection completed, runtime acceptance open

See [component evidence](restricted-sessions-implementation.md). Existing registry atomically reserves dispatch key→session/generation and persists sequence/stages/cancel/cleanup; dedicated class/egress and SDK runner contain no user resources/catalog or publication authority. Private polling/artifact protocol is real. At this earlier checkpoint Stage C still needed to replace the approved `CodingSessionToolHostImpl.authorizeRequestBuild` UNKNOWN_RUN seam. The Stage-C section above records its subsequent replacement with durable lookup/current authority/readiness; live activation remains blocked.

Protected checks: **661 Sessions tests**,12 new real-workerd cases +4 pure policy/runner cases, full dual-version ordinary bridge fixture matrix; actual generated Pi0.85.1 SDK program with local fake Responses endpoint passed (1056 SDK files verified;38 transitive host/image mismatches, so not an image check). `pnpm build` and fresh exact `pnpm test` pass (191 root +3296 package /7 existing skips); final root lint passed (136 warnings/0 errors). Final verifier checked71338 immutable dependency/shim files, reviewed patch24+2 topology, U1/HEAD/unassigned sections/empty index. Failure chronology/logs under `/tmp/odie-restricted-sessions-evidence/`; no suppression or rewritten failure claims.

Owner-approved image-only Pi0.84.2→0.85.1 and OpenCode1.18.15→1.18.30 pins/lock/overlay were regenerated with installed Arborist in protected scratch, no install/scripts/root graph writes. Prime0.9.4 official artifacts verified after an R2 HTTP403 but retained0.8.0 because kernel/MCP2 migration is outside scope. No Sandbox/native upgrade, checked image digest change, container build/publish or live provider operation. Independent review precedes next source stage.

### Admin management UI — implemented; supplied independent review PASS

See [component evidence](admin-management-ui-implementation.md). The existing AdminPage now calls real list/resolve/grant/revoke/audit APIs, shows all configured static seeds and pending blockers, and enables mutations only when the server reports managed mode. No optimistic revocation claims or UI readiness override. Exact identities, explicit confirmation, stale revisions, same-key uncertain retries, private paged audit, self/last-admin messages and page-owned disposal have 23 new UI tests. Full frontend **82 files / 684 passed**, fresh uncached four-command build, direct shared/backend/provider/authority typing, **22** workerd authority/board/compatibility, Context **49**, JARVIS **39**, root lint rules **0 errors / 136 warnings** pass. Fresh typing revealed the old shared `Service` browser-global gap; supervisor approved its exactly equivalent `Fetcher<AdminAuthorization>` spelling using the existing frontend compatibility mechanism, not `Rpc.Stub` substitution. All failures/logs are retained at `/tmp/odie-admin-ui-evidence/`. No new full root suite/artifacts or live actions. Finance/drain and downstream Auto-Build/PR/Slack remain open.

### Authority core review repairs — locally verified, fresh review required

- Ordinary app listing/open now treats authority outage as no elevated authority, not a failure of the owner's ordinary management UIs. Admin-only apps, AdminApi and board hidden operations still deny; actual private Context create/delete survives the outage.
- Only Context/JARVIS receive admin authorization. Even an upgraded ordinary provider is sent no authorization by User dispatch. `assertCurrent(expectedPurpose)` checks the caller's constant against immutable scope; real Context/JARVIS wrong-purpose calls reject. No boolean downgrade or legacy Finance change.
- The explicit combined test-program typecheck now passes. The harness returns the real `ContextApi` and `Pick<JarvisPolicyApi, "get" | "update">`, removing accidental double Cap'n Web/native result mapping without casts or lost methods/assertions. Supervisor approved a separate Wrangler-generated test declaration program with the actual combined main/SQLite namespaces (and real local PendingLogin binding in both fixture configs). Production `worker-configuration.d.ts` is unchanged. LOADER/BROWSER are type-only declarations for imported, unexercised production paths; absent at runtime, never mocked as working. The existing restore-symbol declaration is reused. See the fixture README for generation/provenance.
- Protected `/tmp/odie-authority-core-evidence/repair0-*`: backend **623 + 6 passed / 4 existing integration skips**; Context **48 + 1 passed**; JARVIS **39 passed**; release/rollout **7 passed**; explicit authority tsc, `pnpm build`, and final `pnpm lint` pass. Initial tsc attempts, two incorrect account-enumeration fixtures, wrong JARVIS script invocation and a missing `export` lint error are retained as failures; final passing logs are distinct. No tests skipped or rejection suppression added.
- Finance arbitrary-descendant containment and legacy drain remain unresolved managed-activation gates. No live actions, new native/dependency patches, UI/Auto-Build implementation or full-goal acceptance claimed. Fresh read-only review is next.

### Authority core — initial implementation evidence (typing failure repaired above)

See [authority-core-implementation.md](authority-core-implementation.md) for authoritative scope, exposure inventory and failure chronology. New source: separate SQLite authority/receipts/audit/generations, native fixed-provenance issuer, all AdminApi reads/writes, board hidden/moderation, protocol-aware User dispatch, Context public management and JARVIS policy. JARVIS discovery stays hidden; independent protocol metadata does not create a nav entry. Legacy boolean providers and Finance descendants remain explicit unfenced inventory. UI and downstream Sessions/build/PR/Slack work remain unimplemented.

Protected evidence `/tmp/odie-authority-core-evidence/`: final backend **623 + 6 passed**, four existing integration skips; includes **9 new workerd tests**. Context **48 workerd + 1 Node**, JARVIS **39 Node**, release/rollout **7 Node**; exact `pnpm lint` (all shipped-source builds/types included) exits0; earlier `pnpm build` exits0. `tsconfig.admin-authority-tests.json` uses actual scoped provider transformers for runtime, but explicit test-program `tsc` exits1 with three cross-package namespace and14 native-to-Cap'n-Web `never` result errors. No typing bypass or fresh native/library research. Fresh root `pnpm test`, isolated production artifact build and live gates NOT RUN for this slice.

Generator/header, export-discovery, public ESM resolution, decorator-scope and fixture I/O ownership failures are preserved rather than counted as passes. Correctly minted native accounts persisted/read back through the real User method; early storage failures were fixture misuse, not a proven platform persistence limit. The final provider fixture is runtime-validated, with no error suppression/delays or in-memory production store. Production provider evidence still denies Finance/drain; no live bootstrap, grant, credential/provider action or deployment. All original unassigned tracked diff sections, U1/dependency configuration and HEAD verified unchanged; index empty. Independent review required before component acceptance.

### Implementation-first architecture — documentation-only checkpoint

- Read current decisions/replan/TODO/status/C1/H0, actual static AdminApi/board/Context/JARVIS, Sessions registry/policy/private service/notifier and both migration histories. Read root/project skills plus absolute Sandbox-next, Durable Objects, Workers and Wrangler skills; task's pinned/no-network restrictions take precedence over generic install/latest-reference advice. Read CI closeout and protected runner/profile before shell inspection; no pnpm/toolchain/provider commands executed.
- Supervisor explicitly approved legacy/prepared/managed semantics and preserving the old boolean provider path **only** as unfenced legacy compatibility. New backend uses capability-aware protocol with trusted explicit support evidence and no fallback on denial/outage. Real transition transactions must obtain backend-owned evidence; production absence of Finance/drain proof blocks activation. No admin boolean, fake attestation, fixture hook in production or effective pre-managed revocation.
- Authored `implementation-first-addendum.md`; updated decisions/replan/TODO/status and ADR precedence/migration language. Planned A–E source/test scopes remain unchecked. No kernel, frontend, provider, tests, configs, generated files, dependencies or native diagnostics edited. Finance remains unresolved, not a blanket implementation blocker; further native research is paused. Independent architecture review remains required.
- Validation: `git diff --check` passed; `git diff --cached --name-only` empty; branch/HEAD remain `feat/community-requests` / `248cc75eda989d5ddc93507518a0bf284065e93f`. Raw tracked diff SHA-256 before/after remains `37fdaf758fdc73971a4b08fa00b63e223ace1bc6d2736aad3d24e0834be7614f`. The parent snapshot starts with a diff-stat and is not byte-identical raw-diff format; it was preserved, not used for a false byte-equality claim. Planning docs are in the pre-existing untracked directory.
- U1 `git hash-object` remains `73c397108e929a228bb92e308aa1379d89d08a9a` / `32b5c317d48da531bbb0313014f82c8e1410d22a`. Python local-link/whitespace checks pass for all seven edited docs and assert key transition/readiness/migration markers. No runtime tests added/run: architecture-only stage, prior local CI success not recounted as component evidence. No index/commit/push/deploy/credential/provider changes.

### U1 — baseline / patch-entry boundary

- Writer scope: only upstream `00da2d66ff5b28c61f4a6c6dd50c783090838239`'s two scheduler test files; branch `feat/community-requests`, HEAD remains `248cc75eda989d5ddc93507518a0bf284065e93f`.
- Independent clone `/tmp/odie-hugin-gates.6MfTWT/research.git` exists with no object alternates; complete two-file upstream diff inspected. Working-file blobs equal U1 parent: `schedule-driver.test.ts` = `32259b732d59ba5ae4bbdfe765a071578c21551f`; `worker.ts` = `718b616691d389ddcacee0c820de8fbb6b5959c5`.
- Baseline `pnpm --filter @gadgets/gatekeeper-scheduler test:run`: exit 0; Worker 7 files / 102 passed / 3 pre-existing skipped; app 3 files / 19 passed. Raw output: `/tmp/odie-hugin-gates.6MfTWT/u1-baseline.log`. Expected negative-path Worker diagnostics: opaque admission rejection (twice), callback rejected, authorization rejected; no failed tests.
- Three pre-existing revocation-interruption skips reference workers-sdk issue #14180; retained unchanged. Local `cloudflareTest` and `scripts/assert-workerd.ts` retained.
- Initial compound `find` inspection was rejected by the shell's RTK wrapper (`rtk find does not support compound predicates or actions`); inspection completed with the provided `find` tool instead. No test/runtime infrastructure failure occurred.

### U1 — writer-exit boundary

- Applied only the exact two-file patch from `00da2d66ff5b28c61f4a6c6dd50c783090838239` (parent `115fede15b932e54055ac2d4627547456550624e`), already present in the independent clone at pinned upstream tip `08afe059f1b9ee37129e0327cde43782870fbb76`. No acquisition/fetch, cherry-pick, commit, staging or other upstream batch.
- `/usr/bin/git apply --check /tmp/odie-hugin-gates.6MfTWT/scheduler.patch` and `/usr/bin/git apply /tmp/odie-hugin-gates.6MfTWT/scheduler.patch`: exit 0. Resulting blobs exactly equal U1: `schedule-driver.test.ts` = `73c397108e929a228bb92e308aa1379d89d08a9a`; `worker.ts` = `32b5c317d48da531bbb0313014f82c8e1410d22a`.
- Post-change `pnpm --filter @gadgets/gatekeeper-scheduler test:run`: exit 0; Worker 7 files / 102 passed / 3 pre-existing skipped; app 3 files / 19 passed. Same four expected negative-path exception diagnostics as baseline; no introduced failures. Raw output: `/tmp/odie-hugin-gates.6MfTWT/u1-post-change.log`.
- `pnpm exec vp run -F @gadgets/gatekeeper-scheduler build`: exit 0; app and Worker typechecks plus app bundle completed (0/4 cache hits). Existing plugin/config deprecation warnings for esbuild options and vite-tsconfig-paths; no upgrades/config edits. Raw output: `/tmp/odie-hugin-gates.6MfTWT/u1-build.log`.
- `/usr/bin/git diff --check`: exit 0. Tracked diff is exactly two files, 106 insertions / 15 deletions; snapshot `/tmp/odie-hugin-gates.6MfTWT/u1-applied.diff`. This progress file remains within the pre-existing untracked `docs/plans/` directory. HEAD/index unchanged; all fork production/config files, runtime assertion, original skips, `.oculus/` and `.pi/` preserved.
- Assertion inspection: only real current-generation server-side disposers increment counters; the five-second hold timer never manufactures disposal counts. The two-second Worker wait rejects missing counts and checks exact equality before returning; old-generation disposals resolve their own holds but cannot satisfy a later test. No mutation-testing claim: only the unmodified upstream patch was executed. Its overshoot check runs once both counters reach target (otherwise the deadline rejects), so the upstream comment's “as soon as” wording is stronger than the implementation when just one counter overshoots.
- No runtime infrastructure failures. U1 writer work is implemented and locally verified, not independently reviewed or full-CI verified. All downstream stories and live enablement remain unimplemented/out of scope for this writer.

### C2 — investigation / blocked writer-exit boundary

- Read implementation-decisions.md and this progress file first; full admin-authority ADR and Finance session/capability inventory inspected. No production authority source, migrations, deployed vars or shared contracts changed. Four configured seeds and authentication provider/alias settings preserved exactly.
- Escalated Finance's arbitrary retained child capabilities before production edits. Supervisor authorized design investigation, then an isolated test-only prototype with the supported public Cap'n Web RpcSession/RpcTransport APIs. Approved admission refinement: one protocol message, including a compound/map payload, is one admission unit; already-admitted messages may finish, fresh post-revoke checks deny. No production wiring approved.
- Restart-only correctness rejected: existing delayed DO abort is not per-admission enforcement; namespace restart acknowledgement does not prove containment of all cross-Worker descendants. Design evidence and required coverage are in the configured authority.md artifact.
- Added isolated prototype files: `packages/workshop-backend/__tests__/authority-prototype/{transport.ts,worker.ts,transport.test.ts,wrangler.jsonc}` and `packages/workshop-backend/vitest.authority-prototype.config.ts`. No default suite/glob, skip, runtime assertion or dependency changes. Native Service and native RpcTarget fixtures remain real workerd capabilities; fixture writes only target local test DO storage. Transport checks both directions and aborts on fixed bounds (32 outstanding messages / 256 Ki UTF-16 code units per direction), rather than allowing unbounded buffering.
- First command `pnpm --filter @gadgets/workshop-backend exec vitest run --config vitest.authority-prototype.config.ts`: exit 1 at startup, `ERR_MODULE_NOT_FOUND: Cannot find package 'miniflare'` (backend does not directly expose the scheduler fixture's dependency). No tests ran. Log `/tmp/odie-c2-authority-prototype.log`. Stopped/escalated; supervisor approved one narrow configuration correction and retry, not dependency changes.
- Corrected config using test-only Wrangler named self-service binding; installed pool's `buildProjectWorkerOptions` explicitly rewrites the configured Worker name to its current runner. No guessed internal constant or import. Same focused command retry: exit 1, real workerd 8 tests / 5 passed / 3 failed + 3 unhandled errors, 3.03 s total / 1.55 s tests. Log `/tmp/odie-c2-authority-prototype-retry.log`.
- Failures were preserved, not suppressed: native argument `.dup()` pass-back failed with `Can't dup an RpcTarget stub as a promise`; the unlimited 1-byte source exhausted the bounded message queue before revocation; the native disposer's fire-and-forget remote counter was not observed (0). HTTP-batch denial also emitted an unhandled future rejection. No positive containment/cleanup claim from five passing assertions.
- Supervisor approved one bounded diagnosis cycle. Added matched native pass-back controls, preserving the same valid `echo(child) { return child.dup(); }` API. Command `pnpm --filter @gadgets/workshop-backend exec vitest run --config vitest.authority-prototype.config.ts -t 'native pass-back control'`: exit 1, direct native control passed; serialized boundary control failed with the same `Can't dup an RpcTarget stub as a promise`, plus one unhandled error; 1.73 s total / 145 ms tests. Eight tests were excluded by `-t`, NOT newly marked skipped. Log `/tmp/odie-c2-authority-pass-back.log`.
- This isolates a real interoperability incompatibility for an otherwise valid native API, not merely a fabricated-email/auth policy issue. No supported per-method normalization hook exists in installed public RpcSession options. Provider-wide API rewrites, private hooks, casts and dependency upgrades are not authorized. Stopped with C2 blocked as directed when a genuine unsupported path remains. Other prototype failures require separate diagnosis: stream flood is bounded fail-closed resource exhaustion (not yet normal-stream coverage); remote disposal-counter absence is not proof disposal never occurred; unhandled future errors remain unsuppressed.
- Production AdminAuthority/grant/list/revoke/audit/bootstrap APIs, complete Finance guarding, Context/JARVIS guarding, additive migrations and downstream UI data are **not implemented**. No targeted production admin/Context/JARVIS tests or builds run; no live cutover/activation evidence exists. Default source remains unchanged, so existing static-admin revocation limitations remain. The failed prototype is isolated and is not production code.

Later entries must distinguish installed code from live enablement (which remains out of scope).

### D/E — writer-entry boundary

- Board-first source inspected: authenticated account DO identity, static `#isAdmin`, AdminApi minting, User private feedback/automation, SQLite DO and real-workerd conventions. C2 prototype and exact U1 files remain untouched.
- Supervisor-approved conservative UI contract: no public names/emails/account IDs; only `isOwn`, vote count and viewer vote. Authored plain text only, no private sidecar or legacy automation call. Moderation/explicit hidden access checks existing static admin per call, not dynamic revocation.
- Approved bounds: title 160/body 8000/details 4000; pages <=50; reads 120/min/account; writes 30/min/account, creates 5/hour, details 30/hour. Durable bounded-key receipts retained indefinitely; conflicting canonical payload rejected; exact retries recheck visibility and return current projection without consuming mutation quota. Frontend should debounce suggestions, not poll aggressively.
- Baseline checks before source edits: shared `pnpm --filter @gadgets/workshop-shared build` exit 0; backend `pnpm --filter @gadgets/workshop-backend exec tsc --noEmit` exit 0; real-workerd `pnpm --filter @gadgets/workshop-backend exec vitest run __tests__/finance-access.test.ts` exit 0, 51 passed. Logs `/tmp/odie-board-{shared,backend,tests}-baseline.log`; existing Finance denial diagnostics present. No staged files.

### D/E — HTTP verification / compatibility sequencing boundary

- Board implementation now present (not yet independently reviewed): documented shared module and AuthenticatedApi methods; private account-keyed SQLite registry; authored-public-only projections/search/details/votes; atomic receipts/quotas; static-admin hide/restore/close/reopen/duplicate with internal audit. Additive `v4-community-requests` in both configs preserves their different v3 histories. No legacy feedback invocation or private ingestion.
- Shared/backend source typechecks pass. `wrangler types` generated namespace evidence, then applied existing `generate-worker-types.ts` mainModule normalization to `./src/server`; raw generator's generated-validation main caused typecheck failures before normalization. Board-specific test-source typecheck config added because normal backend tsc excludes tests; it caught test Env augmentation mismatch, corrected. Scoped lint caught control-regex/array-sort/useless-spread issues, corrected; both targeted test-source tsc and lint now exit 0.
- Test iteration: initial fixture usernames violated password username syntax; then attempted disposal of local RpcTargets (not stubs) failed; both fixture errors corrected. Real-workerd run 3: nine passed, HTTP fixture failed with `internal error; reference = 7v9o025tafqimrh7te85lbm3` because unsupported `{fetch}` option escaped normal source typecheck. Stopped/escalated; supervisor approved supported local global-fetch interception, strict URL check/no network fallback, restore and call-count assertion. No actual provider credentials/actions used. Run 4 found awaiting the intermediate capability after batch completion invalid; corrected without protocol changes. Run 5: 10 assertions passed but exit 1 due independently unhandled denied server future `TypeError: 'listCommunityRequests' is not a function`; NOT green. Logs `/tmp/odie-board-tests{,-2,-3,-4,-5}.log` retained.
- Supervisor explicitly moved minimal same-version Cap'n Web compatibility patch ahead of frontend to unblock this real-wire gate. Bounded public-observation investigation found no fixture/public observation hook for that server future. Read-only diagnosis identifies nonpromise import deserialization incorrectly constructing RpcPromise, not RpcStub. Approved reproducible pnpm patch across runtime bundles; no upgrade/protocol/private hook workaround. Native pass-back controls and unchanged denied-wire test must pass; later RPC stage must reuse this patch, with full C2 containment/stream/disposal evidence still deferred. Snapshot `/tmp/odie-board-before-compatibility.diff` and `/tmp/odie-board-before-compatibility-new-files.tar` capture board partial state.

### D/E + narrowly advanced compatibility repair — writer-exit boundary

- D/E local implementation complete, independent review pending. Shared `community-requests.ts`, authenticated server facade, standalone SQLite registry, additive migrations/generated namespace and release manifest evidence, real-workerd board tests and operational doc are present. UI/C2/admin management/Auto-Build/Slack remain unimplemented. Four seeds, old owner-private feedback retrieval, U1 and all authority-prototype sources remain unchanged. U1 blobs rechecked: `73c397108e929a228bb92e308aa1379d89d08a9a`, `32b5c317d48da531bbb0313014f82c8e1410d22a`.
- Supervisor explicitly authorized two same-version `capnweb@0.12.0` pnpm patch hunks, separately reviewable from kernel code: (1) nonpromise imported refs produce RpcStub instead of RpcPromise, fixing valid native `.dup()` pass-back; (2) ErrorStubHook owns/observes its synthetic rejected promise and returns that IDENTICAL rejected promise, leaving outer caller rejections unchanged. The first hunk alone passed native controls but did not fix HTTP denial. A bounded second experiment fixed the duplicate workerd rejection; supervisor authorized retention pending independent ownership review, not unconditional acceptance.
- Causal correction to earlier diagnosis: denial's stack points to **client error deserialization**, not server evaluation (`serialize.ts:917`). Minimal workerd missing-method + deliberate-throw fixture failed with duplicate unhandled errors even under manual caller try/await/catch; matching Node control passed. With hunk 2, re-await/forward/rethrow preserves the exact deserialized error identity and true-promise argument substitution/pipelining still works. No blanket error handler, catch-and-resolve or production logging was added.
- Permanent compatibility regressions: `__tests__/capnweb-compatibility.test.ts`; preserved native pass-back controls; unchanged board wire-denial assertions. Permanent **intentionally failing** outer-caller diagnostic: `__tests__/compatibility-diagnostics/outer-unhandled.test.ts` and `vitest.outer-unhandled.config.ts`, outside default/prototype globs. Latest explicit diagnostic command exits **1**, with exactly one unhandled `UNOBSERVED_OUTER_CALLER_FAILURE`; this is evidence caller-owned failures remain diagnosable, NOT a passed suite. Instructions/ownership review notes in `patches/README.md`.
- Patch generated via `pnpm patch capnweb@0.12.0 --edit-dir /tmp/odie-board-capnweb-patch` and `pnpm patch-commit ...` (pnpm patch operation, no Git commit/staging). Both hunks/comment consistently present in six ESM/CJS runtime bundles. Patch hash `c0223b64e0bf7a8424b2c70570775ad50876d9cfc5981ddec70e0959d904d8ef`. `pnpm install --offline --frozen-lockfile --ignore-scripts` final exit 0; programmatic normalization proves lockfile identical to baseline after removing patch metadata (no upgrades). Patch installation downloaded zero packages. Bundled source maps still contain upstream source; see patch README.
- Final real-workerd regression command: `pnpm --filter @gadgets/workshop-backend exec vitest run __tests__/community-requests.test.ts __tests__/capnweb-compatibility.test.ts __tests__/finance-access.test.ts __tests__/email-migration.test.ts` exit 0: **4 files / 74 passed**, no Vitest unhandled errors. Board is 11 tests + compatibility 1; existing Finance 51 + email migration 11. Covers two accounts/authenticated non-admin, real HTTP signed-out denial, account-unique concurrent writes/votes, conflicting retry keys, quota rollback/reconstruction, hidden list/get/search/related/details/continuations/replays, public/private projections, moderation/current-state replay, duplicate safety, pagination/input bounds. Expected native DO rejection diagnostics still emitted as in baseline; not suppressed.
- Final native control command: `pnpm --filter @gadgets/workshop-backend exec vitest run --config vitest.authority-prototype.config.ts -t 'native pass-back control'` exit 0: 2 passed, 8 excluded by `-t`. **Full prototype not rerun/claimed passing**: normal stream/backpressure/disposal/containment gates remain for the later RPC stage.
- Final source/test checks: shared `pnpm --filter @gadgets/workshop-shared build` exit 0; backend `pnpm exec vp run -F @gadgets/workshop-backend build` exit 0 (codegen/browser/source tsc); `pnpm --filter @gadgets/workshop-backend exec tsc -p tsconfig.community-requests-tests.json --noEmit` exit 0. Scoped `vp lint` exit 0 with only two existing server no-shadow warnings. Backend build retains existing Vite plugin deprecation warnings.
- Release manifest test initially failed as intended for the new migration and old last-migration assertion. Added explicit full base/prod history assertions, updated expected last migration and regenerated golden; reviewed golden diff is **only** added CommunityRequests migration. Final `pnpm exec node --test scripts/release/manifest-lib.test.ts` exit 0: 5 passed.
- Final logs: `/tmp/odie-board-{regression-final,shared-final,build-final,test-types-final-2,lint-final-2,manifest-final}.log`, `/tmp/odie-capnweb-{native-final,outer-durable-final,frozen-final,observe-node}.log`. Failed intermediate/diagnostic logs remain retained; no test suppression to get green.
- Final HEAD still `248cc75eda989d5ddc93507518a0bf284065e93f`, branch `feat/community-requests`, index empty; `git diff --check` exit 0. No commit/push/merge/deploy/live migration/provider PR/Slack/credential access. Broad full-workspace build/test/lint and Bun runtime checks not run. No production C2/revocation claim; no UI shipped. Next gate: independent board privacy/security and separate dependency promise-ownership review, then frontend.

### Board integration verification — writer-entry boundary

- Read the owner replan first, decisions/TODO, applicable ADR/skill, actual board shared/backend/frontend and root/sidebar/setup seams. Supplied backend and frontend independent reviews PASS; latest full authority prototype remains failing (2 tests + 1 unhandled error per supplied frontend review), not production enforcement.
- Baseline branch/HEAD and exact U1 blobs verified unchanged; index empty. Recorded hashes of the isolated prototype and dependency patch/lock/workspace files for exit comparison; no changes to those are authorized in this stage.
- Concrete board navigation gap: required-connector recovery screen replaces the sidebar but lacks a board link; onboarding similarly lacks an escape to the otherwise prerequisite-free board. Add links and focused regressions without changing setup policy. Extend real-workerd serialized board journeys beyond initial create/denial. Verification commands/results will be recorded at exit, not presumed passed.
- Read-only compound `find` inspection was rejected by the RTK wrapper; supplied find tool succeeded. No test/runtime infrastructure failure or fallback protocol.

### F/G — board frontend writer-exit boundary

- Read the owner-approved replan first, then decisions/TODO, anchor/admin ADR, actual shared/backend contracts and frontend auth/routing/sidebar/feedback sources. Supplied `board-backend-review` result is PASS; this writer did not redo that review. Board-first uses existing static-admin checks only; no C2, dynamic admin management, runner or notification authority was added.
- Implemented `/requests`, `/requests/new` and `/requests/$requestId` with the existing Vite TanStack routing generator (generated `routeTree.gen.ts`, not hand-edited). Pinned Community requests link replaces the old auto-PR feedback modal in expanded/collapsed Chat/Code rails. Root login remains required; board routes bypass onboarding, required connectors and mandatory billing account selection. Missing GitHub/domain/admin status does not block board use.
- Implemented list/detail/search/type/status filters, manual pagination/refresh, feature or newly authored public bug preview/consent, account upvote/unvote, consented public details and debounced/bounded related/duplicate suggestions while drafting/reviewing/submitting. Actual documented backend APIs are used; no backend bridge change. Kumo controls and current tokens; plain React text, labeled native form controls, accessible status/error/disabled states. No private diagnostics/identity/context fields are read into submissions, no raw RPC exceptions are displayed/logged, and no automatic legacy PR/build call remains in the feedback entry point.
- Existing Admin page links to explicit moderation view, with hidden-inclusive reads only for admins and hide/restore/close/reopen/duplicate confirmation controls. Hide covers the entire request/details; no per-detail moderation API is invented. Auto-Build displays a truthful disabled not-ready explanation and the planned `totango/odie-os` / `main` target. Static-admin limitations remain unchanged, with no new revocation claim.
- Legacy private status retrieval is preserved as an explicit owner-only, read-only section on the board with manual refresh and strictly constrained PR links. No availability/domain prerequisite is imposed on the board, historical report is imported, diagnostics are attached or legacy submit method is called. Existing backend private APIs remain unchanged; no legacy-capability cutover occurred.
- AuthProvider retains ownership of the API stub; board methods return value records, with no new child stubs to dispose. Read effects ignore stale results; API/query-scoped cursors reset on replacement; mutation lifetime tokens reject late completions after cleanup/reconnect. Uncertain unchanged submissions/details/moderation reuse the same bounded key; confirmed detail/moderation writes reset it. **Frontend draft/key state is in-memory only**, not durable across navigation/reload: UI asks users to check the board before re-submitting after leaving, while server receipts remain durable. No authored text is persisted to browser storage.
- First targeted run: exit 0, 2 files / 13 passed; jsdom emitted its missing `window.scrollTo` diagnostic from real memory-router navigation. Added explicit test-only scroll mock (no runtime/global error suppression). First full frontend run: exit 1, 656 passed / 1 failed, because the existing Sidebar test still expected the removed private feedback modal button. Updated it to assert always-visible board links in both rail and Chat/Code modes, with no automation-availability/status reads.
- Scoped lint initially rejected missing existing test-file JSX-runtime directives, a mutating test sort, conditional assertions, untyped mocks and ordinary comments immediately documenting exported functions. Corrected to repository conventions/JSDoc, typed mocks, `toSorted()` and unconditional assertions. Final scoped lint exits 0 with one pre-existing AdminPage `resourceKey` scoping warning only. No test skips or weakened denial/runtime assertions added.
- **Final full frontend suite:** `pnpm --filter @gadgets/workshop-frontend test:run` exit 0, **79 files / 660 tests passed**. Log `/tmp/odie-board-frontend-suite-final.log`. Final narrow interactions/auth/sidebar/Admin regression command also exits 0, **6 files / 57 tests passed**, log `/tmp/odie-board-frontend-targeted-final.log`. Tests include signed-out root denial, onboarding/billing/connector bypass, ordinary-account board reads, public bug/feature consent/privacy, matching retry keys and reset after confirmed writes, bounded/debounced suggestions, stale search/API completion rejection, safe text/errors, pagination, votes/details, all five moderation actions and legacy private status.
- **Build/types:** `pnpm exec vp run -F @gadgets/workshop-frontend build` exit 0 (source/config type tasks and production Vite bundle; final run 2/4 cached). Direct uncached `pnpm --filter @gadgets/workshop-frontend exec tsc --noEmit` also exits 0 on final source/tests. Logs `/tmp/odie-board-frontend-build-final.log`, `/tmp/odie-board-frontend-types-final-2.log`. Existing plugin deprecation, mixed passwordHash imports and large-chunk warnings remain; no dependency/config upgrade.
- `git diff --check` exit 0; index empty; branch/HEAD remain `feat/community-requests` / `248cc75eda989d5ddc93507518a0bf284065e93f`. U1 blobs still exactly `73c397108e929a228bb92e308aa1379d89d08a9a` and `32b5c317d48da531bbb0313014f82c8e1410d22a`. Prior backend/shared/capnweb patch/prototype/config changes preserved, not edited in this stage. No install, backend bridge test rerun (no backend bridge changed), commit/staging/push/merge/deploy/live migration/provider write/credential access/real PR/Slack action.
- Frontend operational behavior and limitations: `packages/workshop-frontend/src/community-requests/README.md`. Required next gate is independent frontend/privacy/accessibility/correctness review; this writer's green jsdom suite is not browser-to-live-backend e2e or visual/browser review. Broad full-workspace suites, production C2/full prototype, dynamic admin UI, Auto-Build/Slack and live enablement remain outstanding.

### Board integration verification — writer-exit boundary

- **Milestone:** board is independently usable in implemented source and local fixtures, without dynamic admins/C2, GitHub, domain-specific status or Auto-Build. Supplied backend/frontend independent reviews PASS; independent review of this stage's narrow navigation/test edits remains required. No deployment/live usability claim.
- Concrete repairs only: added `/requests` escape link to actual `OnboardingWizard` and the required-connector recovery navigation (including failed status reads), where those screens replace the sidebar. Links do not complete onboarding or connect services. New actual-wizard/memory-router regression asserts navigation and subscription cleanup; existing recovery-screen tests assert the links. Root tests continue to require login and bypass setup only on board paths; sidebar remains visible in ordinary Chat/Code/collapsed layouts.
- Extended `__tests__/community-requests.test.ts` with an actual multi-account HTTP-batch journey using documented RPC types/pipelining, disposed batch lifetimes and strict local URL interception/no network fallback. It verifies new public bug create/retry, second-account list/get/vote/repeated-vote/unvote/details/search/related, ordinary moderation denial, admin hide/restore/close/reopen and hidden get/details/search/related denial. Existing private projection/new-only/no-backfill, duplicate safety, quotas/pagination and signed-out denial tests remain intact. No production backend/shared API or dependency change.
- Inspected source end-to-end: new submission never calls the legacy PR bot; registry has no private feedback/Reporter/workspace/session capability; old private status still delegates via authenticated User owner identity and is loaded explicitly by its separate read-only UI. Public values expose no private author identifiers and render as React text. Public authored secrets cannot be automatically detected; no promise otherwise. Four static admin seeds unchanged; per-call board moderation checks are not new dynamic revocation.
- `pnpm --filter @gadgets/workshop-frontend exec vitest run src/OnboardingWizard.test.tsx src/RequiredConnectionsGate.test.tsx`: exit 0, **2 files / 18 passed**, `/tmp/odie-board-verify-navigation.log`.
- `pnpm --filter @gadgets/workshop-backend exec vitest run __tests__/community-requests.test.ts __tests__/capnweb-compatibility.test.ts`: exit 0, **2 files / 13 passed** (12 board + 1 compatibility), `/tmp/odie-board-verify-backend.log`. Expected workerd diagnostics from asserted native rejections remain; no Vitest unhandled errors. No protocol, rejection handler, runtime assertion or test exclusion changes.
- Final `pnpm --filter @gadgets/workshop-frontend test:run`: exit 0, **80 files / 661 passed**, `/tmp/odie-board-verify-frontend-final.log` (earlier full run also 661 passed). No browser/server started; jsdom/local fixture-only, not browser-to-live-backend or visual/accessibility acceptance.
- `pnpm --filter @gadgets/workshop-shared build`: exit 0, `/tmp/odie-board-verify-shared.log`. `pnpm exec vp run -F @gadgets/workshop-backend build`: exit 0, 0/4 cached, `/tmp/odie-board-verify-backend-build.log`. `pnpm exec vp run -F @gadgets/workshop-frontend build`: exit 0, production bundle + source/config tasks (2/4 cached), `/tmp/odie-board-verify-frontend-build.log`. Existing Vite/plugin deprecation, mixed passwordHash import and large-chunk warnings remain. No generated route diff introduced in this stage.
- `pnpm --filter @gadgets/workshop-backend exec tsc -p tsconfig.community-requests-tests.json --noEmit`: exit 0, `/tmp/odie-board-verify-backend-test-types.log`. Final direct uncached `pnpm --filter @gadgets/workshop-frontend exec tsc --noEmit`: exit 0, `/tmp/odie-board-verify-frontend-types-final.log`. Initial frontend typecheck failed on the new fixture's invalid null AiGatewayInfo; corrected to `{enabled: false}`. Initial lint caught the new untyped mock; corrected without weakening rules.
- **Root lint failed:** final `pnpm lint:check` exits 1 solely on eight pre-existing `eslint(no-unused-vars)` errors for `using lifetime` in preserved `__tests__/authority-prototype/transport.test.ts` at lines 39,48,86,102,116,136,160,171; `/tmp/odie-board-verify-lint-final.log`. Reported to supervisor; no prototype edits/suppression. Earlier root run additionally caught the now-corrected new fixture mock. `pnpm exec vp lint packages/workshop-frontend/src/OnboardingWizard.tsx packages/workshop-frontend/src/OnboardingWizard.test.tsx packages/workshop-frontend/src/RequiredConnectionsGate.tsx packages/workshop-frontend/src/RequiredConnectionsGate.test.tsx packages/workshop-backend/__tests__/community-requests.test.ts`: exit 0, `/tmp/odie-board-verify-scoped-lint.log`.
- `git diff --check`: exit 0. U1 blobs still `73c397108e929a228bb92e308aa1379d89d08a9a` and `32b5c317d48da531bbb0313014f82c8e1410d22a`. `shasum -c /tmp/odie-board-verify-preserved.sha` verifies all five authority-prototype files plus capnweb patch, lockfile and workspace metadata unchanged. Branch/HEAD unchanged, no staged files. No install, commit, push, merge, deploy/live migration, provider write/real PR/Slack or production credential read/mint. Existing checkout `.oculus`/`.pi` preserved.
- Remaining full-goal gaps: root lint above; full workspace build/test/full pnpm lint not run; full C2 runtime prototype not rerun here and remains failing per supplied review (2 tests + 1 unhandled error). Complete containment/stream/disposal evidence, production C2/dynamic admin management and cutover, Auto-Build/control plane/public runs/Slack, budget/credentials/provider prerequisites and live migrations remain deferred. Frontend uncertain drafts/retry keys remain in-memory across mounted reconnect only; no reload/navigation durability. Search scans and indefinite receipt/audit retention remain growth costs. Board pass does not unblock managed authority or external automation.

### RPC repair — writer-entry boundary

- Sole writer resumed after supplied board-verify-review PASS. Read replan first, decisions/TODO, authority ADR/blocker, actual public transport/native fixtures, Cap'n Web docs/runtime and existing two-hunk patch. Board/U1/production authority remain untouched.
- Existing reviewed `capnweb@0.12.0` patch already fixes native pass-back and synthetic error ownership; inspected `pnpm help patch` / `patch-commit`. Reuse it, not another upgrade or node_modules-only edit. Dependency checksums saved in `/tmp/odie-rpc-preserved.sha`; U1 blobs match approved values.
- Baseline full real-workerd prototype: exit 1, 8 passed / 2 failed, 1 unhandled error; `/tmp/odie-rpc-baseline.log`. Failures remain the unpaced infinite stream hitting the fixed bound and fire-and-forget disposal counter staying zero. Preserved original fixture snapshot `/tmp/odie-rpc-prototype-before` before repairs. No infrastructure failure.
- Next: isolated fixture/lifetime/paced-stream and explicit rejection-ownership repairs plus compatibility coverage. Production C2 remains unimplemented and blocked pending complete evidence and independent review; no board milestone rollback.

### RPC repair — blocked writer-exit boundary

- **Status: partial repairs/evidence implemented; C2 containment BLOCKED, production C2 absent.** Supplied final board review PASS retained. No board/shared/production authority/config/U1 source edits in this stage. Existing four static seeds, account identity, main target, board participation and no historical private backfill remain unchanged. No provider/credential/PR/Slack/deployment actions, commit/staging/push/merge or model calls.
- Existing exact `capnweb@0.12.0` patch reused, NOT replaced/upgraded: two reviewed hunks still own native import RpcStub semantics and private synthetic rejected-promise observation. `shasum -c /tmp/odie-rpc-preserved.sha` verifies patch, lockfile and workspace metadata unchanged. `pnpm install --offline --frozen-lockfile --ignore-scripts`: exit 0 (`/tmp/odie-rpc-frozen-install.log`). No new dependency/source-map/type-declaration patch. Cause/upstreamability remains documented in `patches/README.md`; added current prototype blocker link.
- Changed only isolated prototype/evidence: `worker.ts` now creates native children in the fixture DO; disposer synchronously updates actor-local storage. Bounded exporting-context `waitUntil` resolves on real disposal (5s deadline only ends the hold, never fabricates counts). Exact target counts 1/2/3 cover disposed parent, retained/returned independent aliases, last duplicate and session abort. Ordinary source is driver-paced; explicit-start infinite flood stays separate under unchanged 32-message/256 Ki UTF-16 limits. Reader/writer operation/closed promises are observed immediately without global suppression. Generic reverse local main added using public RpcSession, no protocol inspection or authority chosen over RPC. All unused lifetime bindings repaired by actually using the scoped boundary, not removing cleanup.
- Added `native-compatibility.test.ts` (both directions, awaited vs true-promise pass-back, native RpcPromise/RpcProperty and dup), `native-stream.test.ts` (direct native paced read/cancel, observed destination rejection and graceful source/sink completion), `stream-rejection.test.ts` (clean userspace-source rejected-stream control), actual local accepted WebSocket-pair grandchildren and missing-authority tests. Preserved nested/service grandchildren, callbacks, pass-back/provider `.dup()` contract, parent disposal, generations/regrant/outage, map, deferred result and HTTP batch tests. HTTP mock now strictly checks the test URL and call count. `env.d.ts` derives fixture bindings from actual `PrototypeEnv`; explicit `tsconfig.authority-prototype-tests.json` exposes previously unrun test typing. Added prototype README; updated authority-blocker/replan.
- **Full runtime prototype remains FAIL:** final `pnpm --filter @gadgets/workshop-backend exec vitest run --config vitest.authority-prototype.config.ts` exits 1: 4 files / **19 assertions passed + 3 unhandled errors**, `/tmp/odie-rpc-prototype-final-3.log` (final repeat; same failures as final-2). `PROTOTYPE_ADMIN_REVOKED` and `PROTOTYPE_BACKPRESSURE_LIMIT` are extra native stream rejections despite observed operation/closed promises. The third is `PROTOTYPE_NATIVE_PIPE_FAILURE` from a direct native stream with no Cap'n Web import/boundary; caller's `pipeTo` rejection is asserted, but an additional rejection occurs. A userspace stream through the same checked boundary rejects without an extra event. Stream reader rejection text is the library's disposal error, not the authority error; ordinary retained-call probes verify actual abort reason. No complete containment claim from passing assertions.
- Supervisor authorized one bounded native-stream diagnosis, then wrap-up without speculative runtime patching. Inspected shipped Cap'n Web `src/rpc.ts`/`src/streams.ts` via source maps, public types and upstream 0.12.0 workerd tests (read-only raw GitHub fetch to `/tmp/odie-rpc-upstream-workerd.test.ts`). Cap'n Web already observes its pipe pump; direct native rejection establishes a path below this boundary, not an identified private orphan promise. No precise supported fix found. **Do not suppress it or invent another Cap'n Web patch.**
- **Prototype typecheck remains FAIL:** `pnpm --filter @gadgets/workshop-backend exec tsc -p tsconfig.authority-prototype-tests.json --noEmit` exit 1, `/tmp/odie-rpc-types-final-2.log`: six TS2345 errors (direct-native pass-back at transport.test.ts:25 as well as serialized pass-back/callback/native-compatibility inputs), one TS7006 (map callback inference at :97). Native `Unstubify` at generated types :12999 and Cap'n Web input mappings at dist/index-workers.d.ts:65-80,125 unwrap to target rather than accepting this async stub shape. A public `Pick` experiment did not solve sync/async/private shape mismatch and was removed. Supervisor explicitly approved preserving exact legitimate API plus failed type evidence, not casts, mirrors, ts-ignore, signature weakening or skipping the check. Successful runtime controls are not type-safe production compatibility.
- Intermediate runs retained, not counted green: `/tmp/odie-rpc-repair-{1,2,3}.log` (fixture byte Buffer-vs-Uint8Array assertion corrected; stream error assertions corrected to actual disposal errors and exact call probes); type iterations `/tmp/odie-rpc-types-{1,2,3,4}.log` (proper Cloudflare.Env augmentation/include added, unsuccessful Pick removed). New native property fixture initially used an unsupported value-array map path and then was corrected to an actual native getter/RpcPromise/dup control; failed logs `/tmp/odie-rpc-native-compatibility.log`, `/tmp/odie-rpc-prototype-final.log` retained. No failing stream/pass-back scenario was deleted.
- **Narrow passing evidence:** native control filter exit 0, 2 passed / 17 excluded by `-t`, `/tmp/odie-rpc-pass-back-final.log`; both-direction/native-promise regressions exit 0, 2 passed, `/tmp/odie-rpc-native-compatibility-final.log`; userspace stream control exit 0, 1 passed, `/tmp/odie-rpc-stream-isolated.log`. Direct-native stream command exits **1**, 3 assertions passed + 1 unhandled error (`/tmp/odie-rpc-native-stream.log`), not green. Board + reviewed dependency regressions `pnpm --filter @gadgets/workshop-backend exec vitest run __tests__/capnweb-compatibility.test.ts __tests__/community-requests.test.ts`: exit 0, 2 files / 13 passed, `/tmp/odie-rpc-board-compat-final.log`; expected native rejection diagnostics retained, no Vitest unhandled errors.
- **Broad build/lint:** `pnpm build` exit 0, 87 tasks / 3 cache hits, `/tmp/odie-rpc-root-build.log`. Final full `pnpm lint` exit 0 (root lint:check + scripts typecheck + shipped-code build), `/tmp/odie-rpc-root-lint-final.log`; build part 58/87 cache hits. Existing warnings remain (shadow/scoping, deprecated Vite plugin options, mixed passwordHash imports, large chunks). Earlier root lint:check also passed; eight inherited prototype unused-lifetime errors are repaired. Root build intentionally does not include the isolated failing prototype tsconfig; no broad type-success claim for that fixture.
- **Broad test routing failure, stopped/escalated before retry:** `pnpm test --concurrency-limit 2` exit 1, `/tmp/odie-rpc-root-test.log`: unchanged root scripts **189/189 passed**, but trailing option was forwarded to Vitest (`CACError: Unknown option --concurrencyLimit`), four package tasks failed startup with no tests. This was an invocation error, not ignored test failure. Inspected installed `vp run --help` and root package script. Supervisor approved corrected same-protocol package-half retry; captured tool-compressed tracked diff summary plus complete prototype copy under `/tmp/odie-rpc-before-package-retry*` before it. The shell wrapper compresses `git diff` output, so that text snapshot is not a byte-comparable patch; no whole-board checksum assertion is based on it. A separate read-only `require('capnweb/package.json')` inspection failed `ERR_PACKAGE_PATH_NOT_EXPORTED`; package file/source reads succeeded, no runner fallback.
- **Broad package tests corrected command:** `pnpm exec vp run --concurrency-limit 2 --filter '!cloudflare-os' --cache test` exit 0, **31 tasks, 0 cache hits; 264 files / 3180 passed / 7 existing skipped**, `/tmp/odie-rpc-root-package-test.log`. Includes frontend 80/661, backend unit 52/614 and integration 2 files / 6 passed / 4 existing skipped, scheduler Worker 102 passed / 3 existing skipped and app 19 passed. Combined evidence is passing root scripts + corrected package half; the original `pnpm test` invocation remains failed. Default suites deliberately do not run isolated prototype/outer-caller diagnostic configs. Existing negative-path diagnostics and an unlocated RPC-stub-disposal warning (not attributed to baseline) appear in broad backend output; no Vitest unhandled error/failure in the passing package command.
- Final scope/integrity: U1 blobs remain `73c397108e929a228bb92e308aa1379d89d08a9a` / `32b5c317d48da531bbb0313014f82c8e1410d22a`. Branch/HEAD unchanged; `git diff --check` exit 0; index empty. Checkout `.oculus`/`.pi` untouched. Independent review of new isolated fixture/docs work required. Broad build/test artifacts are local only, not deployment/provider changes.
- Remaining gates: native stream rejection ownership, exact native public type interoperability/map inference, permanently unresolved-check cleanup, durable restart/generation and complete callback/subscription/stream lifetimes, capacity/latency evidence, production Finance owner/direct-share guard regression and legacy capability drain. C2/dynamic admin management, Auto-Build/public runs/Slack and live rollout remain unimplemented. Board is retained independently; no whole-goal or managed-authority readiness claim.

### CI recovery continuation — resumed writer-entry boundary

- Owner resumed after provider usage-limit interruption; same tool protocol, branch/HEAD and empty index verified. Existing board, U1, reviewed patch, failing native-stream reproducer and explicit prototype type failures are preserved; no reapplication or production C2 wiring.
- Read actual `.github/workflows/ci.yml`: Ubuntu / Node 24.19.0 runs `pnpm lint:check`, `pnpm types:scripts`, `pnpm build`, `pnpm test`. Prior logs for build/lint, 189 root scripts and corrected package-test half remain present. Local environment is Darwin / Node 24.14.1, so passing local equivalents are not remote CI/CD or exact-platform acceptance.
- `deploy-production.yml` additionally builds artifacts with `node scripts/build-production-deploy.mjs --out production-deploy`; its 16 Wrangler calls are explicit `deploy --dry-run`. Live uploads, secret verification/sync, current-main GitHub checks and origin checks are not authorized. Supervisor approved attempting only sanitized local artifact generation after isolation preflight, otherwise reporting it unrun.
- Preparing an isolated current-source snapshot with explicit new files, no env/credential/personal config contents, clean HOME/config/cache and offline frozen same-version dependencies; no original checkout/environment-file resolution or network/provider access permitted. Installed Wrangler help/source inspected for dry-run semantics and telemetry/env-loading controls. No Wrangler skill is exposed among this checkout's available `.agents/skills` (only write-gatekeeper); no skill installation or alternate agent/tool protocol attempted. Artifact build not yet run or claimed passed.

### CI recovery continuation — infrastructure-blocked exit / actual workflow coverage

- **Stopped before artifact generation; no new runtime/source fix.** Isolated snapshot: `/private/tmp/odie-ci-recovery-46npkv13/source`, 1482 current working-source files (tracked source plus explicitly enumerated new board/prototype/patch files). Source manifest: `source-manifest.json`; excluded-name manifest: `excluded-paths.json`. Personal `.npmrc`, env/dev-vars paths, `.pi`/`.oculus`, credentials and original build/cache outputs were not imported from the checkout. Available project skill inventory contains only write-gatekeeper; installed Wrangler help/source and actual build commands were inspected instead of installing skills.
- Isolation controls: `/tmp/odie-ci-recovery-run.sh` uses an explicit clean `env -i`, temporary HOME/config/cache/TMPDIR and copied Node 24.14.1/pnpm 11.17.0 tools. `sandbox.sb` denies ALL network and all original `/Users/jacob_1` reads/writes; `WRANGLER_SEND_METRICS=false`, `CLOUDFLARE_LOAD_DEV_VARS_FROM_DOT_ENV=false`, empty user/global npmrc, offline package configuration. Copied package cache only, no production tokens/auth files. Original source resolution during a build would be denied, not silently followed. Neither workspace build nor artifact command was reached.
- First isolated command: `/bin/sh /tmp/odie-ci-recovery-run.sh <snapshot-base>/tools/pnpm install --offline --frozen-lockfile --ignore-scripts --store-dir <snapshot-base>/store`. **Tool timeout at 240s, not a captured numeric process exit.** Log `logs/install.log` starts `Verifying lockfile against supply-chain policies (1001 entries)` and attempts registry metadata GETs despite offline flags; network denial yields ENOTFOUND/retries. No successful download or provider access. No matching owned process remained after timeout. Stopped/escalated; did not disable supply-chain policy or permit network.
- Supervisor explicitly approved isolated reuse of current installed dependencies instead of clean-install parity. `/tmp/odie-ci-recovery-reuse.py` copies only the installed dependency symlink closure, excludes env/personal config/cache paths, relocates generated `.bin`/installation metadata only, and requires all links to resolve inside snapshot plus identical Cap'n Web runtime and lock/patch bytes before building. Runtime package code is not patched. First attempt **exit 1**, `logs/dependency-reuse.log`: refused collision with partial offline-install per-package `node_modules/wrangler`. No build occurred.
- Supervisor approved one same-copier cleanup/retry. Moved 34 explicit root/top-level-package node_modules entries lexically inside snapshot source to `partial-reuse-1/`, without symlink traversal or touching original dependencies (`quarantine-inventory.json`); prior root remnants are `failed-install-node_modules/`. Retry **tool timeout at 240s**, empty `logs/dependency-reuse-retry.log`; no graph-completion manifest was produced and no matching owned process remained. Precise copy-versus-audit subphase was not logged, so it is not inferred. **Stopped; no third attempt, competing retry, new runner, sandbox bypass, downloads or upgrade.** `provisioning-status.json` records both timeouts with null process exit code, collision exit 1, zero artifacts and artifact command not-run.
- Actual local CD command `node scripts/build-production-deploy.mjs --out production-deploy`: **NOT RUN**. All 16 outputs (15 configured workers including router, plus router-native) remain unbuilt in this sanitized snapshot. No invocation of Wrangler deploy, upload, secret sync/list, live origin check, CI dispatch or provider credential access. Source helper's internal Wrangler calls were inspected and are explicit `--dry-run`; that is not evidence those checks passed.
- **Reverified retained actual CI evidence, not rerun after the interruption:** `ci.yml` lint job runs `pnpm lint:check` and `pnpm types:scripts`, both covered by prior passing full `pnpm lint` (`/tmp/odie-rpc-root-lint-final.log`); build job's `pnpm build` passed (`/tmp/odie-rpc-root-build.log`). Its `pnpm test` is covered only as two local halves: 189 root script tests passed within the overall FAILED `pnpm test --concurrency-limit 2` invocation, then supervisor-approved `pnpm exec vp run --concurrency-limit 2 --filter '!cloudflare-os' --cache test` exited 0, 31 tasks / 264 files / 3180 passed / 7 existing skipped (`/tmp/odie-rpc-root-package-test.log`). Reparsed raw log confirms these counts and no Vitest unhandled-error summary. The failed original command is still failed, and literal unmodified `pnpm test` was not rerun. Log paths/hashes/counts: snapshot `prior-ci-evidence.json`.
- **CI/CD parity limits:** GitHub `ci.yml` and production artifact build job use Ubuntu / Node **24.19.0**; local checks use Darwin / Node **24.14.1**. CI dependency setup/clean install is not proved by existing local dependencies or the timed-out isolated install. Lock-resolved local Wrangler is **4.120.0**; the production upload job separately installs global **4.119.0**. No versions changed to align them. Remote CI, artifact upload/download, current-main/CI-status checks, production secret checks/sync/deployment/origin verification, preview/provider comments, contribution-policy provider writes, native signing/release and image publishing were **not run**. Local green build/test halves are not “all CI/CD green.”
- **Actionable next infrastructure step, requires another approved bounded attempt:** preserve the snapshot/quarantine manifests and failed logs; inspect copier progress/performance or provision a relocatable installed graph using a bounded filesystem-copy mechanism, verify every workspace/dependency/native-binary link and generated shim stays inside snapshot, then run isolated `pnpm build` (CI prerequisite) and the exact artifact generator under the unchanged no-network/no-original-home sandbox. Do not use the current partial graph as if its missing audit passed or skip pnpm policy to obtain a clean-install claim.
- **Actionable native runtime continuation:** preserve `native-stream.test.ts` (no Cap'n Web imports) as the minimum observed native `pipeTo` destination-rejection reproducer, with direct read/cancel/graceful-completion and userspace-source controls. Trace the pinned workerd native readable-transfer / pipeTo abort and promise-ownership path; identify the exact private orphan before any repair. A fixture-level catch, global handler, capnweb-only suppression or dependency upgrade cannot be justified by this evidence. Re-run the direct control plus full prototype with zero unhandled errors before claiming that gate passes.
- **Actionable public type continuation:** preserve direct-native and serialized `.dup()` controls and exact provider signature. Reduce the input-type mismatch against generated native `Unstubify` (`worker-configuration.d.ts:12999,13025`) and Cap'n Web mappings (`dist/index-workers.d.ts:65-80,125`) separately, including nominal/private target fields, callbacks, real RpcPromise substitution and map inference. Any proposed declaration repair must derive real types, preserve local-target and stub acceptance/brands without mirroring interfaces or unsafe casts, and get explicit owner/reviewer approval. Existing scoped tsc remains FAILED: six TS2345 plus one TS7006; prior full runtime remains FAILED: 19 assertions plus three unhandled errors. Their preserved final logs were rehashed in `prior-ci-evidence.json`, not counted passing.
- **Complete goal remains open:** dynamic-admin authority/C2, admin UI, Finance-derived containment/legacy cutover, restricted Auto-Build/control plane/public runs/Slack and live rollout are not implemented or enabled by this continuation. Board-first delivery stays intact. Source replan remains the sequencing contract, not a board-only replacement goal. Fresh review and subsequent approved runtime/type repair are needed before C2 implementation; later production/credential actions require separate authorization.
- Final original-checkout integrity: branch/HEAD unchanged; index empty; `git diff --check` passed. U1 exact blobs and patch/lock/workspace checksums match the prior writer. This continuation edits only progress documentation plus the mandated external findings artifact, and writes sanitized temporary snapshots/logs outside the checkout. No completed board/U1/patch files reapplied or overwritten.

### Private lifetime experiment — writer-entry boundary

- Authorized section 3 only: isolated transport owns both private mains; interrupt active check waits on terminal close/fixture deadline, preserve first reason/bounds and ordinary alias lifetime. No stream workaround, declaration/dependency repair or production C2 wiring.
- Read full authority-path report, replan/TODO/admin ADR and installed public Cap'n Web lifetime implementation. Baseline tracked diff/prototype/TODO snapshots and hashes: `/tmp/odie-lifetime-evidence/pre-tracked.diff`, `pre-prototype/`, `pre-TODO.md`, `pre.sha`; preservation hashes `preserved.sha` cover exact U1, reviewed patch/lock/workspace and generated backend declarations. Index empty; HEAD unchanged.
- Added isolated `lifetime.test.ts` and edited only prototype `transport.ts` so far. Tests not yet run. Public main broken notifications allow both sessions to ingest the original close reason before owned main disposal; notification/disposal is not a native drain acknowledgement. Five-second default and short test overrides are fixture deadlines only, not production policy. New tests cover stalled/late checks, bidirectional writes/callback bytes, original queue bounds, private main disposal and exact native target counters.

### Private lifetime experiment — checked writer-exit boundary (independent review pending)

- **Implemented only isolated section 3 lifetime repair.** `authority-prototype/transport.ts` owns server/client private main stubs, returns ordinary duplicated aliases, interrupts active authority waits on paired close or fixture deadline, rejects active/queued sends once, releases bridge accounting, and prevents late admission. Original fixed bounds and first failure reason retained. Both public broken notifications precede the exactly-once private-main disposals so generic main shutdown cannot overwrite the reason. Returned `closed` acknowledges those public disposal calls, **not** native drain/cancellation. Default 5000 ms and test 25/100 ms are fixture-only, not production policy.
- Added `authority-prototype/lifetime.test.ts` (13 real-workerd regressions) and `tsconfig.authority-lifetime-tests.json`; updated prototype README. Public-only method observation counts both main disposers exactly once. Actual native targets count 2 disposed parents / 4 total, remain usable through ordinary main/parent disposal, and aliases do not add target counts. Pending native calls in both directions settle before unresolved checks; late success/rejection dispatches no write or callback bytes. Direct send/receive tests prove active/queued settlement and original byte/message bounds, zero bridge bookkeeping, idle wakeup and unchanged error identity. Native authority cancellation/durable capacity/subscription accounting remains outside this proof.
- **Final targeted runtime:** `pnpm --filter @gadgets/workshop-backend exec vitest run --config vitest.authority-prototype.config.ts lifetime`, exit **0**, **1 file / 13 passed**, **zero unhandled errors**, 1.04 s total / 189 ms tests. `/tmp/odie-lifetime-evidence/targeted-final.log`. Initial targeted run also passed 13 (`targeted-1.log`). No assertion weakening, skips or global rejection observation/suppression.
- **Final full original prototype config:** `pnpm --filter @gadgets/workshop-backend exec vitest run --config vitest.authority-prototype.config.ts`, exit **1**, **5 files / 32 assertions passed + the same 3 unhandled native-stream errors** (`PROTOTYPE_NATIVE_PIPE_FAILURE`, `PROTOTYPE_ADMIN_REVOKED`, `PROTOTYPE_BACKPRESSURE_LIMIT`). `/tmp/odie-lifetime-evidence/full-prototype-final.log`, 1.39 s total / 666 ms tests. The original 19 assertions remain present and pass, but this is not green/full containment. Earlier full run has identical counts (`full-prototype.log`). No stream workaround or runtime patch was attempted.
- **New lifetime/source types:** `pnpm --filter @gadgets/workshop-backend exec tsc -p tsconfig.authority-lifetime-tests.json --noEmit`, exit **0**, `/tmp/odie-lifetime-evidence/lifetime-types-final.log`. Config retains `src` and real fixture Env/types, deriving all RPC surfaces. First config omitted source ambient declarations and failed (`lifetime-types.log`); corrected include, not a source/type suppression.
- **Full prototype typecheck remains FAILED:** original command `pnpm --filter @gadgets/workshop-backend exec tsc -p tsconfig.authority-prototype-tests.json --noEmit`, exit **1**, `/tmp/odie-lifetime-evidence/full-types-final.log`. Final full glob reports **9 diagnostics in existing tests** (7 TS2345, 2 TS2339 on `nested.items` typed `never`); no diagnostics in lifetime test/transport. Do not misreport this as the unchanged prior 7 or a native declaration repair. Adding the new test's instantiations changes existing mapped-type diagnostic shape.
- Matched original-file-set type control: scratch `/tmp/odie-lifetime-evidence/baseline-types.json` excludes **only newly added lifetime.test.ts**, recreating the old test set (not a shipped/default test exclusion). Same installed `tsc -p <that config> --noEmit` with exact pre-edit transport and then current transport each exits **1**, identically **6 TS2345 + 1 TS7006** at the original sites; logs `baseline-types-2.log` and `current-original-types.log`. Pre-edit source was restored only for this serial comparison, with a shell trap restoring current source; no original test changed. First scratch config failed relative `types` resolution (`baseline-types.log`), corrected to exact absolute existing type paths; no runner/dependency change. Full repo prototype config remains untouched and still includes every test.
- Intermediate type failures retained: `full-types.log` exposed loss of generic polymorphic-this `dup()` result to `StubBase<unknown>`; `full-types-2.log` rejected a naive result annotation. A tiny private helper constrained by the **actual** `RpcStub<unknown>` now returns the concrete stub subtype through its public `dup()` API; no hand-written mirror, cast, TS-ignore, signature weakening or declaration patch. `full-types-3.log` and final have the 9 old-test diagnostics described above. This helper is not a native/Cap'n Web type-interoperability repair.
- **Scoped lint:** `pnpm exec vp lint packages/workshop-backend/__tests__/authority-prototype/transport.ts packages/workshop-backend/__tests__/authority-prototype/lifetime.test.ts`, exit **0**, `/tmp/odie-lifetime-evidence/lint-final.log` (initial also passed). No root CI/lint/build/test or artifact-install retry in this lifetime-only stage. The partial `/private/tmp/odie-ci-recovery-46npkv13` remains untouched and NOT build-ready; literal `pnpm test`, sanitized artifact generation and remote CI/CD remain unproved.
- Scope/integrity evidence: `/tmp/odie-lifetime-evidence/{pre-tracked.diff,pre-prototype/,pre-TODO.md,pre.sha,preserved.sha,stage.diff,post.sha,integrity.log,type-comparison.json}`. Exact U1 and two-hunk Cap'n Web patch/lock/workspace/generated declarations preserved; original tracked diff byte-identical at exit. Only prototype transport/test/README, dedicated type config and this TODO authored. No production C2/import, seed/auth/config change, dependency version change, install, stream/native declaration edit, provider/PR/Slack action, credential read, Git staging/commit/push/merge or deployment.
- **Remaining gate:** independent review of this bounded lifetime repair. Complete native stream/type compatibility, downstream authority-work capacity/drain, production Finance/C2/dynamic admins, admin UI, restricted Auto-Build/Slack, artifact build and CI/CD are still incomplete. Board milestone and exact U1 remain retained; this stage is not full containment or full-goal completion.

### Native / boundary type experiment — blocked writer-exit boundary

- Read replan/TODO/full authority-path section 4, admin ADR, public declarations/type generator and applicable skills. Supplied lifetime-review PASS retained. No production C2 wiring, provider signature changes, dynamic-admin/UI/Auto-Build work, board/U1 reapplication or runtime/dependency version changes.
- Added isolated `__tests__/authority-types/{native,boundary,owned-projection,recognition}.ts`, two explicit type configs, README and inert rejected-candidate diffs. Tests derive actual public RPC types, keep private targets and legitimate `.dup()` inputs, reject unrelated/lookalike/native-promise inputs, check exact output types, and require unannotated callback/map inference with explicit `IsAny` assertions. They are compiler reproductions, not default-suite exclusions or claimed-green tests.
- **Native-only cycle:** fresh runtime-only baseline exits **1**, two diagnostics (stub input and double-wrapped return); same-version workerd `worker.mjs` three-algebra-arm candidate exits **0** on those positive/negative controls. One native candidate only. `native-baseline-minimal.log`, `native-candidate.log` under `/tmp/odie-types-evidence/`.
- **First boundary candidate failed:** a common ownership projection derived with `Omit<StubBase<T>, "onRpcBroken">` did not actually match native stubs because inherited `dup()` still returns the original Cap'n Web stub. Public non-assignability/return-key assertions prove this (`owned-projection.log`, exit **0**). Full prototype still failed with eight diagnostics. All experimental hunks were restored before further work; failed diff/logs retained, not installed as evidence.
- **Explicit supervisor-approved ONE corrected discriminator candidate:** project only actual public `__RPC_STUB_BRAND` and disposal keys, preserving real output Stub types and all methods. Recognition gate **exit 0**, eight positive/negative assertions BEFORE applying normalization (`recognition-final.log`). Corrected candidate plus unchanged native algebra: native tsc **exit 0**, and **exact** `pnpm --filter @gadgets/workshop-backend exec tsc -p tsconfig.authority-prototype-tests.json --noEmit` **exit 0** (`full-corrected-exact.log`). This removes all original diagnostics, not merely shipped-source errors.
- **New acceptance blocker preserved:** `tsconfig.authority-types-boundary.json` under corrected candidate exits **1**, exactly **TS7006** (unannotated callback `value` implicitly any) and **TS2322** (`IsAny` assertion requires false but inferred true), `boundary-corrected.log`. Common-brand recognition now works; the native callable-stub/local-function union loses callback contextual typing. No callback annotation, cast, mirror, error directive, negative-test relaxation or third candidate was added. Broader collection/function negative acceptance remains incomplete. Exact old-config success is not complete type acceptance.
- **Runtime:** corrected-candidate full original prototype command exits **1**, **5 files / 32 assertions passed + 3 unhandled native-stream errors** (`PROTOTYPE_NATIVE_PIPE_FAILURE`, `PROTOTYPE_ADMIN_REVOKED`, `PROTOTYPE_BACKPRESSURE_LIMIT`), `runtime-corrected-full.log`. Earlier restored-dependency filter `-t 'native pass-back|native promise/property|callback-exported|entire map message'` exits **0**, **6 passed / 26 command-filter exclusions**, `runtime-controls-restored.log`. All original provider signatures and real-workerd assertions retained; no stream workaround/suppression. C2 remains blocked.
- Candidate generation used `pnpm patch` edit directories and **fresh Wrangler output paths** with clean HOME/scratch configs, unchanged dates/flags and existing project postprocessing. The corrected exact check temporarily used a complete freshly generated sibling backend declaration with the existing `ensureRestoreExport` function; no giant declaration was hand-edited. Workerd binary hash stays `3da61644318c8fab32e68a504513865aef12329b1356d75d7e6f83a713ea9f7b`. No durable patch-commit/install: final `worker.mjs`, all six Cap'n Web declaration variants and original generated backend declaration are restored **byte-for-byte**; reviewed runtime hunks/patch/lock/workspace/U1 hashes pass `restored-final-integrity.log`.
- Failed setup commands are not hidden: initial minimal config omitted source ambient declarations while importing the generated project header; replaced its type input with fresh runtime-only generation. First full scratch config guessed the pool type path; corrected to the actual public export's file. First recognition negative incorrectly used a non-stubable DurableObject class; corrected to a real native RpcTarget. Logs/exits retained in `exits.log`; none was an infrastructure failure or an alternate runner retry.
- Restored canonical native reproduction exits **1** with two diagnostics (`native-restored.log`); restored unchanged full prototype tsc exits **1** with the original nine diagnostics (`full-restored-final.log`); restored boundary reproduction exits **1**, six diagnostics (`boundary-restored-final.log`). Final scoped lint and `git diff --check` exit **0** (`lint-final.log`, `diff-check.log`); index empty and tracked diff byte-identical to stage entry. Final restoration hashes, pre/post tracked diff, stage diff, command logs, fresh generated files and full six-variant rejected diffs are in `/tmp/odie-types-evidence/`; source discoverability/commands in `__tests__/authority-types/README.md`. Root build/test regressions were **not run** because no declaration change is retained; root CI/test/artifact evidence is not advanced by isolated compiler experiments.
- **Next gate:** independent review of the type evidence and a separately approved narrow callback-mapping direction, alongside unresolved runtime stream ownership. No automatic third type candidate, production C2, dynamic admin panel, restricted runner/Slack, live provider/credential/PR action, install/version upgrade, staging/commit/push/merge/deploy or artifact-provisioning retry. `/private/tmp/odie-ci-recovery-46npkv13` remains untouched and NOT build-ready; literal `pnpm test`, full CI/CD and complete goal remain open.


### Same-runtime native stream matrix — checked diagnostic exit, repair BLOCKED

- Read replan/TODO/full authority-path section 2, applicable skills/admin ADR and prior lifetime/type/CI evidence. Supplied lifetime/type reviews PASS only their partial stages. Sole writer retained board, exact U1, two reviewed Cap'n Web runtime hunks, accepted private lifetime and all rejected type evidence. No production C2, native/declaration/dependency repair or version/flag change.
- Added isolated `__tests__/authority-native-matrix/{provider.ts,scenarios.ts,worker.ts,matrix.test.ts,wrangler.jsonc,prepare-standalone.mjs,run-local.py,README.md}`, explicit `vitest.authority-native-matrix.config.ts` and `tsconfig.authority-native-matrix-tests.json`. Original native baseline and every original prototype file unchanged. Default test glob unchanged. The small provider retains Service → SQLite DO → native child → native streams, driver-paced chunks and actual-disposer five-second hold; counters add observation only. Explicit pump is diagnostic, not a provider/bridge replacement.
- **Exact original baseline** under same installed pool: exit **1**, 3 assertions pass + 1 `PROTOTYPE_NATIVE_PIPE_FAILURE` unhandled error, `exact-baseline.log/json`. First instrumented pool run exited **1**, 3 failed/1 passed + 1 error, but new cancellation expectations fired before full metadata recording. Supervisor approved ONE instrumentation correction/rerun preserving expectations. Final `pool-corrected.log/json`: **3 failed/1 passed + 1 unhandled**, with all four full outcome records in JSON `meta.nativeMatrix`. Failed source/logs retained in `first-run-source/` and `pool-matrix.*`.
- **Standalone setup stopped/escalated twice, no silent retries:** first public config rejected `network.deny=[public,private]` before startup (`startup-failed.log`, `rejected-config.capnp`, exit **1**). Supervisor approved ONE terminal deny-Worker correction. Second launch started but public `inMemory` storage lacked actual `storage.kv` despite `enableSql=true`; all four requests were already issued by the harness before their recorded setup failures were inspected. Those four are invalid-lifetime evidence, NOT acceptance (`invalid-inmemory/`). Supervisor approved ONE source-backed `localDisk` SQLite correction, same-class storage/write/readback/real-disposer readiness, and immediate per-result infrastructure stop. No source counter rewrite or fake timer completion. Installed Miniflare's normal SQLite uses `localDisk` (`dist/src/index.js:104130`), consistent with public schema `workerd.capnp:681–706,847–891`.
- **Final eight matched outcomes:** pool and standalone each produce exactly **1 extra unknown-promise rejection for pipeTo**, zero for explicit pump/cancel/graceful. Both two-chunk failure cases preserve **identical original caller error**; explicit pump observes read/write/closed, counterpart cancel/abort, releases locks and rethrows. Every final scenario has actual native child disposer **1** and no caller-assertion/cleanup failure. In BOTH contexts every producer cancellation callback count is **0**, reason **null**, before disposal and after actual disposal + 50 ms reporting turn; graceful correctly has no cancellation and writes once. Pipe, explicit-pump and cancel therefore remain failed diagnostic propagation expectations (HTTP **500** standalone); graceful passes (HTTP **200**). Counts are measurements, not a complete native drain guarantee or source-backed permission to relax cancellation. Final elapsed 52–71 ms, positive remaining five-second cleanup budget. Final same-class preflight passes with actual write/readback/disposer1.
- No pool-versus-standalone discriminator, so **zero production-flag controls** (of at most two) were justified/run. Operation discriminator narrows investigation toward native pipe shutdown/transfer; neither Vitest nor Cap'n Web is necessary for the extra event. Public non-suppressing listener records promise identities, never prevents default/attaches handlers from events; caller errors stay observed and assertions explicit. Unknown event identity is NOT the private owner and no supported patch line is identified. No native C++ source acquisition/build, library workaround/global suppression or hosted-runtime claim. This is rejection/lifetime noise/incompleteness, not evidence of successful post-revoke access or proof of security escape.
- **Execution isolation:** existing native binary hash `3da61644318c8fab32e68a504513865aef12329b1356d75d7e6f83a713ea9f7b`, unchanged `2026-02-02`/`nodejs_compat`; pool 0.20.3 and same workerd binary. Fresh final fixture `/tmp/odie-native-matrix-NBz5yd/` copies only binary and three test modules, local SQLite directory, no dependency install. Explicit named `internet` is a deny Worker with no imports/bindings/network service; both Workers explicitly route outbound there. Terminal self designation never executes recursion (only throws). Original-home/external-network sandbox denial retained; fresh explicit HOME/config/cache/environment, loopback pre-bound descriptor only. Startup60s/case15s/cleanup5s bounds. No credentials/env files/production bindings/providers read or used. Standalone stderr empty is not a pass: JSON records the extra event and driver exits **1**. Process cleanup is bounded and intentional SIGTERM is recorded separately.
- **Final full original runtime rerun:** exit **1**, **5 files / 32 assertions pass + 3 unexpected unhandled errors**, `full-prototype.log/json`; same `PROTOTYPE_NATIVE_PIPE_FAILURE`, `PROTOTYPE_ADMIN_REVOKED`, `PROTOTYPE_BACKPRESSURE_LIMIT`. **Exact original prototype tsc** exits **1**, **7 TS2345 + 2 TS2339**, `full-types.log`. These are NOT green C2 gates. New dedicated matrix tsc and scoped lint exit **0**, `types-final.log`, `lint-final.log`; first new tsc's wrong module Env augmentation failed (`types.log`) and was corrected to existing global Env pattern. No source/declaration/type-signature suppression.
- All actual evidence is under `/tmp/odie-native-matrix-evidence/`: `commands.jsonl` (full argv/exits), `exits.log`, setup failures, final per-cell JSON, pre/post tracked diffs, TODO copy, preserved hashes, stage diff and final hashes. Durable source commands/limits/failure interpretation are in the matrix README. `run-local.py`'s final refusal-to-overwrite/preflight-timeout hardening was syntax checked, not another runtime matrix retry. Executed `run.py` retained separately. `git diff --check` passes, index empty, baseline branch/HEAD unchanged; tracked pre/post diff remains byte-identical. No staging/commit/push/merge/deploy/credential/provider/PR/Slack action, production config/seed/auth edit, root CI/artifact retry or reuse of unaudited snapshot.
- **Next gate:** independent review of this bounded blocked diagnostic. A precise native-transfer/pipe ownership source explanation/repair remains prerequisite; no speculative same-version patch is authorized from the matrix alone. Type callback inference, complete cancellation/drain/capacity, Finance-derived C2/admin authority, admin UI, restricted Auto-Build/Slack remain blocked/unimplemented. Literal `pnpm test`, sanitized artifact build, remote CI/CD and full goal remain incomplete. `/private/tmp/odie-ci-recovery-46npkv13` is preserved and NOT build-ready.

### CI closeout — recovery incident and incomplete gates (chronological checkpoint)

- Read the full CI review/authority-path, replan/TODO/ADRs and absolute Wrangler skill. Existing board, exact U1 and production configuration remain unchanged. Evidence: `/tmp/odie-ci-closeout-evidence/`; pre-stage tracked diff and TODO saved there. No artifact provisioning/build has been attempted in this stage.
- **Accidental dependency recreation:** the first literal `pnpm test`, with clean HOME and `CI=true`, triggered pnpm 11.17's default automatic install before tests. Saved workspace state omits `enableGlobalVirtualStore`, while CI sets it to false; pnpm compares those values and calls install. The changed HOME also changes the store path. Pnpm recreated node_modules before its supply-chain metadata requests were denied. The 1200-second bound terminated the owned group (process exit null/timeout, driver70); no test ran. Root dependencies fell from 71,344 files to286; tracked source/lock/patch stayed byte-identical. This was an execution/setup mistake, not merely an unavailable artifact snapshot. No package install was explicitly requested and no network access succeeded.
- Supervisor-approved read-only recovery audit and restoration used old `failed-install-node_modules` payloads plus `partial-reuse-1` metadata/links. Fresh independent real copies, per-file store hashes/modes, lock peer topology and internal link checks; damaged32 existing module directories quarantined, two originally absent module directories restored. Recovery directory `/private/tmp/odie-dependency-restore-gmf_mgyo/` records all34 roots/rollback state. Initial preflight wrongly assumed all34 existed and stopped before copying; approved correction preserved that failed staging/log. A post-swap executable check found missing package shims; after separate audit/approval,338 valid preserved shims were restored from the old retry source. Two obsolete transitive workerd shims targeting non-current versions were deliberately not copied; no source/script explicitly references them. No install/version change or policy weakening used for recovery.
- **Correction/retraction of initial recovery acceptance:** the per-package audit incorrectly permitted the unpatched **base** variant for the c022-patched Cap'n Web directory. Therefore its initial “exact reviewed runtime verified” conclusion was false, despite correct lock/path identity and correct workerd hashes. Installed six runtime bundles were restored as unpatched base files. This is a recovery audit bug, not a new board defect. All six wrong bytes/hashes are preserved; `/tmp/odie-ci-closeout-evidence/capnweb-restoration-audit.json` now proves the exact c022 CAS candidates equal the base plus ONLY the two reviewed hunks in all six bundles (12 hunks, both Git blob IDs and SHA-512 verified);15 other package files/declarations/maps unchanged. At this entry, copying those six corrected bundles awaits supervisor approval. Do not treat the restored graph as compatibility-accepted yet.
- Protected exact-command attempts after recovery never auto-installed: clean HOME, explicit original store, CI omitted (local—not CI-mode parity), dependency write/unlink/rename/replacement guards, denied external network and credential/env reads. First executed retry exits1:183 root tests pass/5 fail because blanket guards denied generated test `.env` fixtures and the repository's own policy `.npmrc`. Non-disclosing validation subsequently proved that root file equals HEAD and contains only the expected public @cloudflare registry directive; personal configs remain denied. Temporary-fixture-only env exceptions restored root tests to189/189; the188 count previously substituted one failed-file outcome for two unloaded registry tests.
- Next literal retry exits1 before package tests: Vite+ IPC bind EPERM. One approved, supported no-op task probe established `VP_RUN_IPC_NAME` is a Unix socket under the explicit fresh TMPDIR; observed socket removed at exit. Inside/outside owned-socket canaries prove the narrow exception without opening unrelated sockets. An earlier disposable file-protection canary caught a `/tmp` versus `/private/tmp` profile mismatch before pnpm ran; canonicalized rules then proved content write, unlink, directory rename and file replacement denial while permitting scoped scratch writes.
- Latest literal `pnpm test` with those protections and owned-TMPDIR IPC: **exit1**,103.28s, root189/189 pass; package run reports30 tasks/1 failed. Backend52 files/614 assertions pass but **6 unhandled rejections**, including board/compatibility failures caused by the wrongly restored unpatched runtime above. This is FAILED evidence, not CI acceptance; no assertions, skips, `assert-workerd`, default task or suppression changed. Raw logs: `test.log`, `test-protected.log`, `test-protected-corrected.log`, `test-protected-ipc.log`; exact argv/exits in `commands.jsonl`.
- C2/native stream/type acceptance, dynamic-admin backend/UI, restricted Auto-Build/Slack, sanitized16-output artifact build, clean-install/Ubuntu Node24.19.0 parity and remote CI/CD remain incomplete. Local Node24.14.1 Darwin checks and recovery are not full-goal completion. No source/runtime repair beyond restoring previously reviewed dependency bytes is authorized here; no credentials, provider/PR/Slack writes, staging/commit/push/merge/deploy. Old quarantine `/private/tmp/odie-ci-recovery-46npkv13` remains preserved and NOT build-ready.

### CI closeout — local gates checked, independent review pending

- **Final local results:** exact `pnpm test` (NO appended arguments) exits **0**,69.08s: root **189/189**, package **31 tasks /264 files /3180 passed /7 existing skips**,20 task cache hits. Existing board+compatibility smoke first exits0,2 files/13 passed, no unexpected unhandled errors. `pnpm lint` exits0,49.43s (includes lint:check, scripts types and shipped-source build); exact `pnpm build` exits0,11.29s,60/87 cache hits. Logs `/tmp/odie-ci-closeout-evidence/{smoke,test-final,lint,build}.log`; argv/exits in `commands.jsonl`, normalized counts in `test-final-counts.json`. Expected native negative-path diagnostics and existing warnings remain; no skipped tests/assert-workerd/task edits or error suppression. An ANSI-sensitive summary-only prelude assertion failed before the final command; the shell was not fail-fast. Actual smoke exit0 and normalized13/zero-unhandled evidence independently establish its prerequisite; this harness mistake is recorded in `pre-final-summary-check.json`, not hidden or counted as a test failure/pass.
- **Reviewed runtime restored correctly:** six bundles were atomically restored from exact c022 side-effects CAS files, proven equal to the reviewed patch's12 hunks and Git blob IDs. No new compatibility repair or version change. Corrected verifier explicitly rejects the unpatched base alternative.24 workspace links resolve patched0.12;2 legitimate pinned0.8 links (Sessions and ODIE KG) remain unchanged, cross-checked against lock importers. An overbroad all26 assertion failed and was corrected without payload changes. All21 c022 package files, including untouched declarations/maps, verify. `/tmp/odie-ci-closeout-evidence/{capnweb-restoration-audit.json,required-patch-final.log}`; original flawed audit and wrong restored bytes remain preserved. Final original graph check: **71,338 immutable files/shims match corrected expectations**, source tracked diff/U1/lock/patch/native/type-worker unchanged. Recovery is not a clean install; original caches/patch-session scratch remain preserved in the damaged-directory quarantine rather than represented as package payloads.
- **ONE new artifact provision attempt, no installer/copier retry:** `/private/tmp/odie-ci-artifact-ad5o0svu/source` contains1495 sanitized source files, including `COMMUNITY-REQUESTS.md`, all15 new plan docs and the vetted policy-only root `.npmrc`, plus independently copied current dependency/tool files. Original env/dev-vars/personal config/auth files were excluded without importing their contents. Mechanical copied-metadata relocation changes only root/store paths, including workspace-state project keys/patch paths; settings and validation timestamp unchanged, original state untouched. No hardlinks to originals, package install/download, policy disabling or version change.
- Provisioning stopped after materialization when an overstrict audit treated an upstream PNPM tool shim's `# cmd-shim-target` **comment** as an executable escape. Read-only diagnosis verified all8 such tool shims are unchanged; their actual relative execution operands stay inside the copied graph, and selected `bin/pnpm.mjs` imports `../dist/pnpm.mjs`. No tool bytes were edited/discarded to pass. A subsequent full audit identified one genuinely dangling stale hoist link for obsolete `gatekeeper-team-pi`: no current lock/importer/state/.modules/manifest/source reference or package manifest exists, only original leftover build outputs. Supervisor approved removing **only that copied symlink**; no placeholder/orphan contents or metadata edit, original link retained. Audit-only completion of the existing snapshot then passed76,588 file/link rows. No second materialization attempt. Initial failures/incremental journals/provenance are retained in that snapshot and `/tmp/odie-ci-closeout-evidence/`.
- **Isolated actual build/artifacts:** with clean HOME/env, original `/Users` access denied, external network denied, owned-TMPDIR Unix/loopback IPC only, copied dependency/metadata write protections and install-monitoring retained, exact `pnpm build` exits **0**,40.68s,87 tasks/0 cache hits. Then exact `node scripts/build-production-deploy.mjs --out production-deploy` exits **0**,53.98s. Actual Wrangler4.120.0 logs contain **16 `--dry-run: exiting now`** and16 prepared outputs. Artifact root: `/private/tmp/odie-ci-artifact-ad5o0svu/source/production-deploy/`; **16 worker directories /131 files /56,637,767 bytes**. Includes all15 configured workers plus router-native; router assets present, native router has no frontend assets. No deploy/upload/publish, secret operations, provider actions or live migration.
- Artifact logs: `/private/tmp/odie-ci-artifact-ad5o0svu/logs/{isolated-build,artifacts}.log`; `commands.jsonl` records exact argv/exits. `artifact-verification.json` contains every output path/main/file hash; `file-manifest.json`, `audit-files.jsonl`, `relocations.json`, `tool-shim-classification.json`, `orphan-hoist-{provenance,omission}.json`, `sandbox-canaries.json` and `existing-snapshot-audit.json` record actual input/isolation evidence. All **73,725 recorded copied input files** remain byte-identical after both builds; all recorded links remain inside snapshot. Source/progress docs reflect the provisioning boundary; this later closeout entry was written afterward, not silently substituted into the built input manifest.
- **Scope and remaining gates:** local environment remains Darwin/Node24.14.1; CI uses Ubuntu/Node24.19.0. Reused graph runs omit CI because its default changes stored dependency settings; clean-install/supply-chain revalidation/CI-mode parity are unproved, and remote workflow/CD/provider/production execution remain NOT RUN. Production upload's separate global Wrangler4.119.0 versus local4.120.0 is unchanged. Full isolated authority prototype still has documented32 assertions+3 unexpected native-stream errors and failed public native/callback typing; not rerun or relabeled green. C2/dynamic-admin backend, admin management UI, restricted Auto-Build/control-plane/public runs/Slack and rollout remain **open/unimplemented**. Independent review of this recovery/CI evidence is required; neither local gates nor16 dry-run artifacts complete the full goal.
