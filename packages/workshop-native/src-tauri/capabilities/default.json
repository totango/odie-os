{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default-deny Odie OS native API surface for the top-level bundled window only.",
  "windows": ["main"],
  "local": true,
  "permissions": [
    "core:default",
    "dialog:allow-save",
    "deep-link:default",
    "notification:default"
  ]
}
