// Type-checks the tests too. Separate from `tsconfig.json` so `build` keeps emitting only `src`.
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "declaration": false,
    "types": ["./worker-configuration.d.ts", "node"]
  },
  "include": ["src", "__tests__"]
}
