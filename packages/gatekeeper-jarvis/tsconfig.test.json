// Type-checks tests separately so the package build emits only `src`.
{
  "extends": "./tsconfig.json",
  "compilerOptions": { "noEmit": true, "declaration": false },
  "include": ["src", "__tests__"]
}
